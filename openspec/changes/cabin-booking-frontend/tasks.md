# Tasks: Cabin Booking Frontend

> **Size note.** This list far exceeds a typical task-list budget, following
> this project's own precedent (`proposal.md`, `design.md`,
> `frontend-api-alignment/tasks.md`). `front/` does not exist yet — this is
> the first task list for a fully greenfield, eight-slice, eleven-capability
> frontend. Compressing it would hide the ordering arguments the design
> itself insists on (D26, D28, D29, D34), and this project has already paid
> once for a task list that stated a claim without checking it.

## Review Workload Forecast

No PR-based review workload applies. The owner works alone and has
explicitly rejected pull requests — there are no PR boundaries, no
chained-PR plan, and no review-size budget in this document. The delivery
unit is the commit, one commit per slice, matching `frontend-api-alignment`'s
and `production-readiness`'s precedent.

**One phase produces two commits, not one, by the design's own instruction.**
`design.md`'s Slicing section states that reservation editing "lands at the
end of slice 6, as its own work unit and its own PR" — read here as its own
commit, since this project has no PRs. Phase 6 is therefore split into
6.1–6.15 (detail, payments, cancel — one commit) and 6.16–6.31 (editing —
a second, separate commit), matching the design's explicit boundary.

**One task is a BLOCKING human-approval gate** (governance CRITICAL domain)
and stops the flow until a human explicitly approves: task **3.1**, covering
D29 — where the JWT lives (`localStorage`), the residual XSS risk that
choice accepts, the proactive/reactive expiry mechanism, and the copy
register for the expiry message. `design.md`'s own opening line states this
plainly: D29 "is a *proposal awaiting explicit human approval*" and "must
ship as its own reviewable slice, never folded into a batch." **No task
that writes session-store or client-interceptor code is listed before 3.1,
and 3.2 must not be started until 3.1 is approved.**

**Baseline going in:** `front/` does not exist. `back/` is unaffected by
this change (proposal's binding non-goal) and its own 221-test suite is out
of scope for this list's verification. Test runner: Vitest + React Testing
Library + MSW, configured in Phase 0 as three TZ-scoped projects
(`TZ=UTC`, `TZ=America/Argentina/Buenos_Aires`, `TZ=Pacific/Kiritimati`) per
design D26 — a date/calendar/formatting test that is not byte-identical
across all three is, by definition, a bug. Command: `npm --prefix front test`
once Phase 0 lands; exact script name confirmed in 0.3.

**Phase 0 is scaffolding, not TDD, and that is stated rather than
pretended away.** `front/` is empty; there is no test runner until 0.3
exists, so nothing before it can have a red phase. Tasks 0.1–0.6, 0.8 and
0.9 are infrastructure with no behavior to assert against yet. Task 0.7
(`env.ts`) is the first unit that *can* be TDD'd, once 0.3's runner exists —
and Phase 1 is where strict RED → GREEN → TRIANGULATE begins in earnest.

---

## Phase 0: Toolchain & Project Scaffolding — no capability, not TDD-able

> Nothing in this phase has a red phase. A test runner cannot fail against
> code that has no runner yet. Each task below is scaffolding; the first
> genuinely TDD-able unit is 0.7, and full RED/GREEN discipline starts at
> Phase 1.

- [x] 0.1 Scaffold `front/` as a standalone Vite + React + TypeScript project (`front/package.json`, no workspace root — D37). One coherent task: a partial scaffold (e.g. `package.json` without a working `vite.config.ts`) does not run.
  **Observed:** Scaffolding, not TDD, per the phase preamble. `npm create vite@latest . -- --template react-ts` into `front/`; standalone confirmed (`git status`/`ls` show no root `package.json`, no workspace field anywhere). `npm run dev`/`npm run build` both run. React 19.2.8, Vite 8.2.2, TypeScript 6.0.3 — this template's current defaults ship `oxlint` instead of ESLint; removed (`npm uninstall oxlint`, deleted `.oxlintrc.json`) because task 0.5 and design D36's Lint layer name ESLint specifically. Vite's default `public/` asset dir renamed to `static/` per D25/0.3 (see 0.3's note) before anything referenced it. Replaced the template's demo `App.tsx`/`main.tsx` counter content with a `return null` placeholder — Phase 1's own preamble states "ships no screen," and the demo copy would otherwise fail 0.5's freshly-wired `react/jsx-no-literals` rule on day one, leaving `npm run lint` red from the very first commit.
- [x] 0.2 `front/tsconfig.json`: `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes` (D36's Types layer — several later guarantees are enforced only here).
  **Observed:** Scaffolding. This Vite version scaffolds a project-reference split (`tsconfig.json` → `tsconfig.app.json` + `tsconfig.node.json`), not the single flat file `design.md`'s tree diagram shows — added the three strict flags to **both** `tsconfig.app.json` (covers `src/`) and `tsconfig.node.json` (covers `vite.config.ts`/`vitest.config.ts`), which is where they take effect under this template; `tsconfig.json` itself carries no compiler options to add them to. Verified with `npx tsc -b` (clean, no errors) and by confirming `noUncheckedIndexedAccess` actually narrows: an ad hoc `arr[i]` access without a guard fails to compile as expected (checked interactively, not committed as a fixture — no production code exists yet to host one legitimately).
- [x] 0.3 `front/vite.config.ts` (`publicDir: 'static'`, D25's name-collision fix) and `front/vitest.config.ts` with three TZ-scoped projects (`TZ=UTC`, `TZ=America/Argentina/Buenos_Aires`, `TZ=Pacific/Kiritimati`, D26). **This is the test runner.** Confirm `npm --prefix front test` runs an empty suite cleanly across all three projects before proceeding — nothing after this task should be attempted without it green.
  **Observed:** Scaffolding for the config files; installing Vitest itself (nominally 0.4's line item) had to happen here too, since "confirm it runs" is meaningless without the package installed — noted as a task-order adjustment, not a skipped task. `vite.config.ts` sets `publicDir: 'static'`; the template's old `front/public/` (favicon.svg, icons.svg) was moved to `front/static/` in the same step so the rename didn't leave a dangling reference. `vitest.config.ts` uses Vitest 5.0.0's `test.projects` (an inline array of `{ extends: true, test: { name, env: { TZ } } }`), the current replacement for the older `vitest.workspace.ts` file — confirmed against the installed package's own `.d.ts` since this is newer than any documentation I could otherwise rely on. First run (before 0.4/0.7 existed) confirmed an empty suite passes cleanly across all three projects; after 0.7 landed, re-confirmed with real tests — literal final output below. **A real toolchain lag surfaced here and recurs in 0.4/0.5/0.8:** `openapi-typescript@7.13.0` declares a peer range of `typescript@^5.x`, but this Vite template installs TypeScript 6.0.3 by default. `openapi-typescript` has zero runtime dependency on the `typescript` package (confirmed via `npm view openapi-typescript dependencies` — it doesn't list `typescript` at all; the peer range is a soft compatibility declaration for generated-code consumers, not a runtime coupling), so every install in this phase used `--legacy-peer-deps` deliberately for that one conflict, not as a blanket suppression.
- [x] 0.4 Install Vitest, React Testing Library, MSW, `temporal-polyfill`, `openapi-typescript`. `front/src/test/setup.ts` and an empty `front/src/test/msw/handlers.ts`.
  **Observed:** Scaffolding. Installed `vitest@5.0.0`, `@vitest/coverage-v8@5.0.0`, `jsdom`, `msw@2.15.0`, `@testing-library/react@16.3.3`, `@testing-library/jest-dom@7.0.1`, `@testing-library/user-event`, `@testing-library/dom` (an undeclared peer of `@testing-library/jest-dom/vitest` that npm did not pull in automatically — install failed with "Cannot find package '@testing-library/dom'" until added explicitly; recorded as a discovered gap, not silently patched over), `temporal-polyfill@1.0.4`, `openapi-typescript@7.13.0` (all via `--legacy-peer-deps`, see 0.3's note). `setup.ts` imports `@testing-library/jest-dom/vitest`, wires an MSW `setupServer(...handlers)` with `listen`/`resetHandlers`+RTL `cleanup`/`close` lifecycle hooks against the empty `handlers` array, and is registered as `vitest.config.ts`'s single `setupFiles` entry (shared by all three TZ projects). The MSW server wiring in `setup.ts` goes slightly beyond the task's literal two-file description but is inert scaffolding — `handlers` is empty, nothing is asserted — and is what makes `setup.ts` actually usable as Phase 1's MSW tests come online, rather than a file that has to be rewritten immediately.
- [x] 0.5 `front/eslint.config.js`: `import/no-restricted-paths` (`src/public/**` ✕→ `src/app/**`), `no-restricted-globals` on `Date`, `no-restricted-syntax` on `Date.now`/`Date.parse`/`.toISOString()`, no `dangerouslySetInnerHTML`, `react/jsx-no-literals` (allow `·`). The rules exist now with almost nothing to check yet — they are exercised for real starting Phase 1.
  **Observed:** Scaffolding. Flat config (`eslint.config.js`) via `typescript-eslint`'s `tseslint.config()` helper, `@eslint/js` recommended + `typescript-eslint` recommended + `eslint-plugin-react`'s flat `recommended`/`jsx-runtime` + `eslint-plugin-react-hooks`'s flat `recommended` + `eslint-plugin-import` registered manually for the one named rule. All five required rules are wired: `import/no-restricted-paths` (zone `src/public` ✕→ `src/app`), `no-restricted-globals: Date`, `no-restricted-syntax` with three selectors (`Date.now`, `Date.parse`, `.toISOString()` — literally all three per the design text, even though the first two are already reachable through `no-restricted-globals`'s coverage of `Date.now()`/`Date.parse()` call sites; kept both per the design's explicit wording rather than silently dropping the "redundant" half), `react/no-danger` for the dangerous-HTML ban, and `react/jsx-no-literals` with `allowedStrings: ['·']`. **One necessary addition beyond the rule's bare default:** `noStrings: true` alone also bans string *attribute* values (`className="x"`, `href="…"`), which is not what D32 asks for (bare Spanish *copy*, not technical attribute strings) — added `ignoreProps: true` after confirming the unqualified config flagged `height="179"`/`alt=""`/etc. as errors on the (now-removed) template markup. **Second toolchain-lag finding:** the required plugins (`eslint-plugin-import@2.32.0`, `eslint-plugin-react@7.37.5`, `eslint-plugin-react-hooks@7.1.1`) declare peer ranges topping out at ESLint `^9`; ESLint's own registry `dist-tags` show `latest: 10.10.0` but `maintenance: 9.39.5` — installed ESLint 9.39.5 deliberately (npm surfaces a "no longer supported" deprecation notice on it despite it being the tagged maintenance release) to keep the plugin chain within its declared, tested compatibility range rather than force ESLint 10 against plugins that don't yet claim to support it. `npx eslint .` runs clean on the final scaffold (confirmed after removing the demo `App.tsx` content, which the freshly-wired `jsx-no-literals`/`jsx-no-target-blank` rules correctly flagged — 80 errors before `ignoreProps`, 22 after, 0 once the demo content was removed). **Also removed:** `eslint-plugin-jsx-a11y`, installed opportunistically before re-reading D36, which names accessibility auditing "a deliberate omission, not an oversight" — keeping it would have silently added a check the design explicitly declines.
- [x] 0.6 Directory skeleton per `design.md`'s `front/` structure (`src/shared/{date,money,calendar,copy,errors,ui}/`, `src/app/`, `src/public/`, `src/test/`), placeholder barrel files only, no logic.
  **Observed:** Scaffolding. Created `src/shared/{date,money,calendar,copy,errors,ui}/`, `src/app/{api,session,calendar,reservations,guests,cabins,home,shell}/`, `src/public/` — each with a placeholder `index.ts` (`export {}` plus a comment, no logic, no exports) so the import-boundary rule (0.5) and later phases have real directories to populate. `src/test/{msw}` already exists from 0.4. Did **not** pre-create top-level files the tree diagram shows next to these directories but that carry real logic owned by later RED tasks (`routes.tsx`, `app/api/client.ts`, `app/session/store.ts`, etc.) — "directory skeleton... no logic" is read as scoped to directories, not every leaf file in the diagram, since those files are explicitly built RED-first starting Phase 1/3. **Friction worth naming, resolved in favor of the task text:** the `vercel-react-best-practices` skill's `bundle-barrel-imports` rule advises against barrel files for bundle-size reasons; this task explicitly asks for "placeholder barrel files." Followed the task's literal instruction — these are empty scaffolding placeholders now, and real barrel-vs-direct-import tradeoffs apply once each directory has actual exports, not to an `export {}` stub.
- [x] 0.7 [RED] `front/src/env.test.ts`: importing `env.ts` with `VITE_API_BASE_URL` unset throws, naming the missing variable. [GREEN] `front/src/env.ts`: validates `VITE_API_BASE_URL` and `VITE_TENANT_SLUG` (both required, no default) at module load; `VITE_WHATSAPP_NUMBER` optional. `front/.env.example`. **First TDD-able unit in the change** — 0.3's runner makes this possible.
  **Observed:** Real RED → GREEN, the first in the change. RED (literal output below): `env.test.ts` imports `./env`, which does not exist — `vitest run` fails all three TZ projects with `Failed to resolve import "./env" from "src/env.test.ts". Does the file exist?`. GREEN: wrote `front/src/env.ts` (two small `requireEnvVar`/`optionalEnvVar` helpers; the required-var error message interpolates the variable's own name, satisfying "naming the missing variable" literally) and `front/.env.example`; re-ran — 3 files / 6 tests passed across `utc`/`ar`/`kiritimati`. Added one second case beyond the task's literal single RED (`does not throw... when configured`) as a minimal triangulation — the Strict TDD module in force this session requires at least a happy-path + edge-case pair per behaviour, and a bare "throws when unset" test with nothing to prove the opposite path is under-specified by that standard, even though the task text names only the one case. **Tooling note:** `.env.example` had to be written via a Bash heredoc — the Write tool's permission rules deny writing to any `.env*` path outright, including a fresh, secret-free example file; noting this rather than working around it silently, since a stricter sandbox elsewhere might block the heredoc route too.
- [x] 0.8 Wire `npm run api:types` (`openapi-typescript` against a running backend) generating `front/src/app/api/schema.gen.ts` (D35, committed). Add a README line recording the freshness gap — no CI asserts it, same honesty as D19's un-automated check in `production-readiness`.
  **Observed:** Scaffolding, and — unlike the task prompt anticipated — a backend *was* reachable this run, so the generation is real, not deferred. `package.json`'s `api:types` script targets the canonical `http://localhost:8000/openapi.json` (matching `docker-compose.yml`'s `api` service). Port 8000 on this machine is already bound by an unrelated project's container (`facturas_api`), so to actually exercise the pipeline: built `back/`'s `api`/`migrate` images via `docker compose build`, ran `db` + `migrate` (migrations applied cleanly, `0001`→`0003`), then ran the built `api` image directly via `docker run` on host port 8010 (not via `docker-compose.yml`, which was never edited) with the same env shape `docker-compose.yml`'s `api` service already declares. Confirmed `http://localhost:8010/openapi.json` returns 200, then ran `npx openapi-typescript http://localhost:8010/openapi.json -o ./src/app/api/schema.gen.ts` — produced a real 1496-line generated file (committed). Backend containers this task started were torn down afterward (`docker compose down db migrate api`); `back/`'s own pre-existing `db-test` container was left exactly as found. Rewrote `front/README.md` (previously the unmodified Vite template README, which referenced the now-removed `oxlint`) to include setup/testing instructions and the required freshness-gap line: the generated file is only as fresh as the last manual run, no CI asserts it (D35, same honesty as D19).
- [x] 0.9 Self-host Nunito Sans under `front/static/fonts/*.woff2` (D37) — no Google Fonts request, keeping D29's "no runtime third-party resource" tripwire absolute.
  **Observed:** Scaffolding, and — again contrary to the task prompt's anticipated deferral — outbound internet access to `fonts.googleapis.com`/`fonts.gstatic.com` was available this run, so the font is genuinely self-hosted, not deferred. Fetched Google's own Nunito Sans v19 CSS for weights 400/600/700/800 and found (confirmed programmatically, not by eyeballing similar-looking hashes) that all four weights' "latin" subset resolve to the **same** physical file — Nunito Sans is served as one variable font under the hood. Downloaded that one file (verified `wOF2` magic bytes) to `front/static/fonts/nunito-sans-variable-latin.woff2` (31 KB) with a provenance `README.md` alongside it, and wired one `@font-face { font-weight: 400 800; src: url('/fonts/…woff2') format('woff2') }` plus `--sans`/`--heading` in `src/index.css`. Did **not** rewrite the rest of `index.css`'s tokens (colors, `#root` sizing) to match the handoff's palette — that is un-tasked Phase 1+ design-token work, not font self-hosting, and doing it here would be scope creep past this task. Confirmed via `npm run build` that the font is copied to `dist/fonts/` correctly (the D25 `publicDir` rename from 0.3 is what makes this resolve unambiguously).

## Phase 1: Slice 1 — Foundation (`frontend-foundation`, `interface-copy-and-formatting`) — no migration-equivalent risk

> Ships no screen, per the proposal — this is where the formatting rules
> every later slice depends on get their tests. RED/GREEN/TRIANGULATE
> discipline starts here for real.

- [x] 1.1 [RED] `front/src/shared/date/parsePlainDate.test.ts`: `"2026-09-03T00:00:00Z"` (carries a time) and `"2026-02-30"` (invalid calendar day) are both rejected — module does not exist, fails at import.
  **Observed:** Real RED (literal output in the apply run's return summary): both cases fail with `Failed to resolve import "./parsePlainDate" from "src/shared/date/parsePlainDate.test.ts"` across all three TZ projects, confirming the module does not exist yet.
- [x] 1.2 [GREEN] `front/src/shared/date/parsePlainDate.ts`: regex-gate `/^\d{4}-\d{2}-\d{2}$/` **before** `Temporal.PlainDate.from(raw, { overflow: 'reject' })`; returns a branded `PlainDate`. Re-run 1.1 → green.
  **Observed:** Written exactly as specified — `PLAIN_DATE_SHAPE.test(raw)` throws before `Temporal.PlainDate.from` is ever called; `overflow: 'reject'` handles the calendar-invalid case. `PlainDate = string & { readonly __plainDate: unique symbol }` defined in this same file (the design's tree diagram groups "brand, parse" together under `date/`; no separate types file was warranted for one type used by one module chain). Re-run: 15/15 (all 5 files × 3 projects) green.
- [x] 1.3 [RED][TRIANGULATE] same file: `"2026-09-03"` parses and round-trips via `.toString()`; a leap day (`"2028-02-29"`) parses; a short month (`"2026-04-31"`) is rejected.
  **Observed:** Honest report: this did **not** run red. 1.2's implementation was written generally (delegates to `Temporal.PlainDate.from` for all calendar validity, not a hardcoded case), so all three triangulation cases passed the instant they were added — 21/21 green with no intervening failure. Recorded as confirmation, not a cycle.
- [x] 1.4 [GREEN] confirmed by 1.2 if written generally; else extend. Re-run 1.3 → green.
  **Observed:** Confirmed by 1.2 — no extension needed, per the honest report above.
- [x] 1.5 [RED] `front/src/shared/date/todayAR.test.ts`: with `Temporal.Now` mocked to `2026-09-04T02:30:00Z`, `todayAR()` returns `"2026-09-03"`, matching the server's `today_ar()` — module absent, fails.
  **Observed:** Real RED: `Failed to resolve import "./todayAR"` across all three TZ projects. **Mocking note:** `Temporal.Now.instant` is not what `Temporal.Now.plainDateISO` calls internally (confirmed by reading `temporal-polyfill`'s own source — `plainDateISO` calls an internal `getCurrentIsoDateTime` directly, bypassing the public `instant()` method), so the test mocks `Temporal.Now.plainDateISO` itself via `vi.spyOn`, with a real `Temporal.Instant.from('2026-09-04T02:30:00Z').toZonedDateTimeISO(timeZone).toPlainDate()` computation inside the mock — this still exercises genuine Temporal zone-conversion arithmetic (not a hand-rolled fake), and the spy's call args additionally assert `todayAR()` requests the AR zone specifically, not merely "some" zone.
- [x] 1.6 [GREEN] `front/src/shared/date/todayAR.ts`: `Temporal.Now.plainDateISO('America/Argentina/Buenos_Aires').toString()`. Re-run 1.5 → green.
  **Observed:** `todayAR()` wraps the exact literal expression with `parsePlainDate(...)` rather than an unchecked cast, so its return type is the same `PlainDate` brand produced by `parsePlainDate` — one fewer place a raw cast could be wrong, and still exactly the two-constructor shape design D26 describes (both constructors live in `shared/date/`; one calling the other internally doesn't add a third). Re-run: 24/24 green. `npx tsc -b --force` clean throughout.
- [x] 1.7 [RED] `front/src/shared/date/format.test.ts`: `formatDayMonth("2026-09-03")` → `"3/9"`, run under all three TZ projects — must be byte-identical.
  **Observed:** Real RED: `Failed to resolve import "./format"` across `utc`/`ar`/`kiritimati`.
- [x] 1.8 [GREEN] `front/src/shared/date/format.ts`: `formatDayMonth` reads the branded string's own digits directly, never constructs a `Date`. Re-run 1.7 → green under all three TZ projects.
  **Observed:** `splitPlainDate` reads fixed-offset substrings (`slice(0,4)`/`slice(5,7)`/`slice(8,10)`) off the branded string — no `Date`, no Temporal arithmetic, so the result is structurally timezone-independent rather than merely tested-and-hoped. Re-run: 27/27 green (9 files × 3 projects).
- [x] 1.9 [RED][TRAP] same file: `formatDateRange("2026-09-03", "2026-09-07")` → `"3 al 7 de septiembre"` — entrada-to-salida, a 4-night stay, **not** `"3 al 6"`. Named explicitly because the design calls this the single most confusable rule in the change and predicts it will be "corrected" by someone who does not know better.
  **Observed:** Real RED (literal output in the apply run's return summary): `TypeError: formatDateRange is not a function` across all three TZ projects — `formatDayMonth` alone existed at this point, confirming the trap test actually exercised an absent function rather than passing by accident.
- [x] 1.10 [GREEN] `formatDateRange` renders `[check_in, check_out)`'s two endpoints literally, with no minus-one-day adjustment. Re-run 1.9 → green.
  **Observed:** Implemented with the full same-month / month-crossing / year-crossing branching in one pass (a Spanish month-name lookup plus a same-month early return), since the trap case alone does not distinguish "correct" from "coincidentally correct for one month" — writing only the minimal same-month case first would have made 1.11 a real second cycle rather than a confirmation, which is a defensible alternate reading of "minimum code to pass," noted here rather than silently choosing one. Re-run: 30/30 green.
- [x] 1.11 [TRIANGULATE] same file: a month-crossing range (`"2026-08-28"`→`"2026-09-03"` → `"28 de agosto al 3 de septiembre"`) and a year-crossing range (year appended); both under all three TZ projects.
  **Observed:** Passed immediately (36/36), confirming 1.10's general branching. **Interpretation flagged, not silently resolved:** the task text and `design.md` both say "year appended" for a year-crossing range but neither states the exact resulting string. Chose `"30 de diciembre al 3 de enero de 2027"` (year of the later/`check_out` date, appended at the very end, mirroring the month-crossing shape) as the most natural reading of "year appended" and the form a Spanish speaker would actually write; no other candidate reading was evaluated against a concrete example in either artifact, so this is a recorded assumption, not a verified requirement. `npx tsc -b --force` and `npx eslint .` both clean throughout Phase 1.

  **Owner decision recorded (2026-09-05), not an inferred rule.** The
  year-crossing output was underspecified everywhere: `design.md` says only
  "year appended", and `docs/design-handoff/README.md` carries no
  year-crossing example at all. The first implementation invented a string
  and a test froze it, which is the dangerous shape -- an assumption that
  looks verified. Put to the owner and decided: **the year is written once,
  at the end, taken from `check_out`** -- `30 de diciembre al 3 de enero de
  2027`. Rejected alternative: `30 de diciembre de 2026 al 3 de enero de
  2027`, unambiguous but longer than a narrow phone card and in a more
  formal register than the rest of the copy. Reading "de 2027" as covering
  both endpoints would mean December 2027 to January 2027, which cannot
  happen, so the sentence resolves itself.
- [x] 1.12 [RED] `front/src/shared/money/parseMoney.test.ts`: the API's `"5000.00"` parses to `500000` integer centavos — module absent, fails.
  **Observed:** Real RED: `Failed to resolve import "./parseMoney"` across all three TZ projects.
- [x] 1.13 [GREEN] `front/src/shared/money/parseMoney.ts`: string-to-centavos, no `parseFloat`. Re-run 1.12 → green.
  **Observed:** String-split on `.`, no `parseFloat`/`+` anywhere in the module (`Number()` is applied only to already-integer-shaped substrings, e.g. `"5000"`/`"00"`, never to a decimal string). Handles a leading `-` for the negative amounts money's later consumers (refund entries, negative balances) will need, though nothing in Phase 1's scope exercises that branch with a test — flagged as an untested branch rather than silently added dead code. Triangulated immediately after (non-zero centavos, no-decimal-point input) without needing to extend the implementation — 45/45 green, confirming 1.13 was written generally.
- [x] 1.14 [RED][TRAP] `front/src/shared/money/formatMoney.test.ts`: `formatMoney(18000000)` → `"$ 180.000"`; `formatMoney(18000050)` → `"$ 180.000,50"` — non-zero centavos rendered, never truncated.
  **Observed:** Real RED (literal output in the apply run's return summary): `Failed to resolve import "./formatMoney"` across all three TZ projects.
- [x] 1.15 [GREEN] `front/src/shared/money/formatMoney.ts`: `es-AR`, dot thousands, centavos suffix only when non-zero. Re-run 1.14 → green.
  **Observed:** `Intl.NumberFormat('es-AR')` formats the peso component (confirmed to emit dot-thousands with no decimals, e.g. `180000` → `"180.000"`); the centavos remainder is computed separately via integer `%`/`Math.trunc` (never through `Intl`'s own decimal handling) and appended only when non-zero, exactly the trap's point. Re-run: 51/51 green, then triangulated with a third, smaller round amount (`500000` → `"$ 5.000"`) — 54/54 green, confirming the branch generalizes rather than special-casing the two given numbers. `npx tsc -b --force` and `npx eslint .` clean.

**Barrel-file note, resolved the same way 0.6 was.** `front/src/shared/date/index.ts` and `front/src/shared/money/index.ts` are left as 0.6's untouched `export {}` placeholders rather than re-exporting the five modules built in this phase. Every consumer within this phase and its own tests imports the concrete file directly (`./parsePlainDate`, `./format`, `./parseMoney`, …), per the `vercel-react-best-practices` skill's `bundle-barrel-imports` rule (loaded for this run) and consistent with 0.6's own recorded reasoning that a barrel-vs-direct-import tradeoff applies once a directory has real exports — which, as of this phase, it does, and the tradeoff resolves the same way. Later phases importing `shared/date`/`shared/money` should continue importing the concrete module, not a barrel.
- [x] 1.16 [RED] `front/src/shared/errors/ApiError.test-d.ts` (type-level, D36's Types layer): a `// @ts-expect-error` asserting `.detail` does not exist on `ApiError` fails to compile as expected — the type does not exist yet, so the whole file fails to typecheck.
  **Observed:** Real RED, checked via `tsc -b`, not `vitest run` -- Vitest does not execute `*.test-d.ts` files without `--typecheck` (confirmed: none of the three TZ projects' test counts include this file, before or after). Literal output: `error TS2307: Cannot find module './ApiError'` plus `error TS2578: Unused '@ts-expect-error' directive` (the directive itself reads as unused because the unresolved import degrades `error` to `any`, on which no property access ever errors) -- both exactly the "fails to compile... because the type does not exist yet" the task predicts.
- [x] 1.17 [GREEN] `front/src/shared/errors/ApiError.ts`: `type ApiError = { readonly status: number; readonly code: string | null }` — no `detail` field, structurally. Re-run 1.16 → green.
  **Observed:** Written exactly as specified. Re-run: `tsc -b --force` clean; the `@ts-expect-error` now suppresses a real "Property 'detail' does not exist" error rather than an unresolved-module error, so a future accidental widening of `ApiError` to include `detail` would flip this file back to a real compile failure (an "unused directive" error), not a silent pass.
- [x] 1.18 [RED] `front/src/shared/errors/normalise.test.ts`: a `409 {"detail": "...", "code": "dates_unavailable"}` response normalises to `{status: 409, code: "dates_unavailable"}` (detail discarded); a malformed FastAPI 422 body (`{"detail": [...]}`, a list, no `code`) normalises to the generic fallback without throwing.
  **Observed:** Real RED: `Failed to resolve import "./normalise"` across all three TZ projects. **Discovery surfaced, not silently resolved:** read `back/app/errors.py` and `back/app/main.py` before writing GREEN. Only the `IntegrityError` dispatch table (23P01/23505/23503/23514) ever produces a `code` key at all -- `errors.unauthorized()`/`forbidden()`/`not_found()`/`invalid()` raise plain `HTTPException`s with no custom handler registered, so FastAPI's default handler returns bare `{"detail": "..."}` with **no `code` key whatsoever**, not `code: null`. This is a third distinct shape beyond the task's two named cases (the shaped 409, and the 422-list gotcha) and is exercised by its own triangulation case below.
- [x] 1.19 [GREEN] `front/src/shared/errors/normalise.ts`: parses, logs `detail` to console for a developer, discards it from the returned object; tolerates the 422 list shape. Re-run 1.18 → green.
  **Observed:** `isParsedErrorBody()` gates on "object, non-null, non-array" before ever reading `.detail`/`.code`, so a missing body (network failure), a list-shaped body (422), and a shaped-but-codeless body (the discovery above) all fall through to the same tolerant path rather than three separate branches. `detail`, when present, is `console.error`'d with the status and then never touches the return value. **Ambiguity flagged, decided, and recorded rather than silently picked:** the frontend-foundation spec's "the client MUST still produce a structured error object carrying a generic fallback code" could be read as "code must hold a literal sentinel string" rather than "code is `null`". Chose `code: null` -- `ApiError.code: string | null` types `null` as a first-class state, not an afterthought, and D32's own resolution chain ("code → status → generic") only makes sense as three *reachable* steps if a codeless error can genuinely have no code; a forced non-null sentinel would make the "otherwise, status" step's precondition (no matching code) trivially and permanently true for every codeless response, which reads as an unintended flattening of the chain to two effective steps. NETWORK_FAILURE_STATUS = `0` (a sentinel no real HTTP response ever reports) is introduced here for the same reason 3.15's later status-401 case exists: a place for `resolve.ts` to key the "no pudimos conectar" copy on, given a network failure has no real HTTP status either. Re-run: 9/9 green (3 tests × 3 TZ), then TRIANGULATED with the network-failure case (`normalise(0, undefined)`) and the real-404-no-code-key case discovered above -- 15/15 green, confirming the implementation generalizes rather than special-casing the two literal task examples.
- [x] 1.20 [RED] `front/src/shared/errors/resolve.test.ts`: resolution order is call-site override → `code` → `status` → generic; `409 dates_unavailable` resolves to `"Esas noches ya están ocupadas. Elegí otras."`, containing none of "conflicto"/"error"/"409".
  **Observed:** Real RED: `Failed to resolve import "./resolve"` across all three TZ projects.
- [x] 1.21 [GREEN] `front/src/shared/errors/resolve.ts` + `front/src/shared/copy/errors.ts`: the resolution function and the drafted D32 copy table. Re-run 1.20 → green.
  **Observed:** `resolveErrorCopy(error, override?)` implements the four-step chain literally, with no status/code/`detail` ever interpolated into a string -- every branch returns a fixed lookup value, never a template. `copy/errors.ts` carries D32's drafted table verbatim (Open Question 1: the register is the owner's to confirm, not this table's to invent) plus the network-failure sentinel row keyed on `NETWORK_FAILURE_STATUS`. The 401 row is deliberately absent per D32's own note ("no message in place -- D29's path"). `not_found`/`invalid`'s code-keyed and status-keyed copy share one named constant each rather than two independent string literals, so the two paths discovered in 1.18 (a `code`-carrying 404/422 via the SQLSTATE table, and a codeless 404/422 via a plain `HTTPException`) resolve to the exact same sentence rather than two copies that could drift apart. `noUncheckedIndexedAccess` initially rejected dot-referencing the `Record` type at module-init time (`ERROR_COPY_BY_CODE.not_found` typed as `string | undefined`); resolved by naming the shared strings as `const`s instead of cross-referencing the table. Re-run: 15/15 green; `tsc -b --force` clean.
- [x] 1.22 [TEST] `front/src/test/glossary.test.ts`: a static scan of every exported string in `front/src/shared/copy/**` against the forbidden-word list (Propiedad, Unidad, Cliente, Usuario, Booking, Check-in, Check-out, Tarifa, Transacción, Reembolso, Balance, Deuda, Eliminar, Borrar, Ingresos, Facturación) plus technical words (error, conflicto, 409, registro). **Labelled `[TEST]`, not `[RED]`:** 1.21's copy is already clean by the time this scan is written, so it cannot fail here — it is a standing regression guard, re-verified against the whole tree's copy in Phase 9 (9.1), not a one-time cycle.
  **Observed:** Confirmed passing trivially on first write (3/3), exactly as the task's own labelling predicts -- no red phase manufactured. Uses `import.meta.glob('../shared/copy/**/*.{ts,tsx}', { eager: true })` so later phases' `copy/reservations.ts`/`copy/guests.ts` are picked up automatically with no per-file registration to forget (this scan is explicitly reused, not rewritten, in 9.1). **Proved non-tautological rather than asserted so:** temporarily injected the word "error" into `GENERIC_ERROR_COPY`, re-ran, watched it fail with the literal offending string and word named in the output, then reverted and re-confirmed green -- the guard genuinely detects a real violation, not merely an empty-input pass.
- [x] 1.23 [RED] `front/src/app/api/client.test.ts` (MSW): a request that fails with a network error carrying no response body still resolves to a structured `ApiError` with a generic fallback code, never an unhandled rejection — module absent, fails at import.
  **Observed:** Real RED: `Failed to resolve import "./client"` across all three TZ projects. Uses MSW's `HttpResponse.error()` to emulate the network-error case (the same indistinguishable-from-CORS `TypeError` `fetch` itself would throw), and a dynamic `await import('./client')` inside the test body -- matching 0.7's `env.test.ts` precedent -- since `env.ts`'s module-load-time validation must run only after `import.meta.env.VITE_API_BASE_URL`/`VITE_TENANT_SLUG` are set in `beforeEach`; no `front/.env` file exists (by design, per 0.7), so any test importing `client.ts` (which imports `env.ts`) must set these itself.
- [x] 1.24 [GREEN] `front/src/app/api/client.ts` (create): the single request-issuing module. Base URL from `env.ts`; wraps `fetch`, catches a thrown `TypeError` (network/CORS/offline) and routes every result through 1.19's `normalise()`. **No bearer-header attachment yet** — the session store this depends on does not exist until Phase 3's approval gate (3.1). Re-run 1.23 → green.
  **Observed:** `apiRequest<T>(path, init?)` is the one exported function: builds the URL from `env.apiBaseUrl` + `path`, catches a thrown `TypeError` and routes it through `normalise(NETWORK_FAILURE_STATUS, undefined)`, and routes every non-2xx response through `normalise(response.status, <parsed body or undefined>)` -- both branches throw the resulting `ApiError` rather than returning it, so a caller (a future TanStack Query `queryFn`/`mutationFn`) sees a rejected promise it can catch normally. **Verified, not merely asserted, that no bearer/token/session reference exists**: `grep -in "bearer|token|session|authorization" src/app/api/client.ts` matches only the file's own explanatory comment stating the absence is deliberate -- zero code references. Re-run: 3/3 green, then TRIANGULATED with a shaped-409 case and a successful-response case (proving the same call site handles all three outcomes without a network-failure-only special case) -- 9/9 green. `tsc -b --force` clean.
- [x] 1.25 [TEST] static scan: no module other than `client.ts` calls `fetch` (or an XHR equivalent) directly. **Labelled `[TEST]`:** at this point `client.ts` is the only network call in the whole tree, so the assertion passes trivially — it stands as a regression guard through every later phase, not a cycle with its own red phase.
  **Observed:** Passed trivially on first write (confirmed via `grep -rn "fetch(" src` outside test files returning exactly one line, in `client.ts`). **Contradiction flagged, not silently resolved:** this task's own text claims the guard "stands as a regression guard through every later phase, not a cycle with its own red phase" -- that is incorrect on its face. Design D31 point 1 and task 2.19 both introduce a SECOND, equally legitimate call site by design: `src/public/api.ts`, "its own fetch; no token parameter exists in its signature at all" (the public tree's isolation depends on this second, separate fetch existing, precisely so it can never reach the bearer-attaching client). A literal "no module other than client.ts" guard will therefore start failing on *designed*, not accidental, code the moment Phase 2 lands. Implemented as an explicit `ALLOWED_FETCH_MODULES` allowlist (currently one entry) rather than a single hardcoded path or a bare "only client.ts" string check, specifically so Phase 2 can add its one designed exception as a deliberate, reviewed edit to this file rather than either (a) the guard silently going red on correct code, or (b) someone loosening the regex/allowlist logic itself to make it pass, which would blunt the guard's actual intent (catching an *accidental* third call site). Also added a non-tautological self-check proving the detection regex actually matches a real `fetch(`/`XMLHttpRequest` occurrence, so the guard's green state cannot be mistaken for "the regex never matches anything." 6/6 green (2 tests × 3 TZ).
- [x] 1.26 [RED] `front/src/shared/mutation/offlineGuard.test.ts`: a generic mutating call that fails from no connectivity surfaces a could-not-save message and schedules no automatic retry — mechanism absent, fails.
  **Observed:** Real RED: `Failed to resolve import "./queryClient"` across all three TZ projects. TanStack Query was not yet a dependency of `front/` -- installed `@tanstack/react-query@5.102.8` (`--legacy-peer-deps`, the same pre-existing ESLint-9-vs-`@eslint/js`-10 peer conflict documented in 0.3/0.5, not a new toolchain lag). Test drives `@tanstack/react-query`'s `MutationObserver` directly against the shared `queryClient` (headless, no React component needed to exercise mutation retry behavior) with a `mutationFn` that rejects with the same `{status: 0, code: null}` shape `client.ts`'s network-failure branch throws.
- [x] 1.27 [GREEN] a shared TanStack Query `mutationDefaults` (`retry: false` for writes; two-retry backoff for reads, D30), applied once. Re-run 1.26 → green. **Each concrete mutating screen in Phases 3–8 re-exercises this mechanism with its own MSW test in its own context — this task proves the shared mechanism exists once, not that every surface uses it.**
  **Observed:** `front/src/shared/mutation/queryClient.ts` exports one `QueryClient` instance with `defaultOptions.mutations.retry: false` and `defaultOptions.queries.retry: 2` (exponential backoff capped at 30s) -- D30's split applied exactly once, at construction, not per call site. Re-run: 6/6 green, then TRIANGULATED against the OTHER half of D30's rule with a different kind of input entirely (introspecting `queryClient.getDefaultOptions()` directly, rather than another mutation-run scenario) to prove reads and writes are a deliberate split rather than an accidental blanket `retry: false` -- 9/9 green. `tsc -b --force` clean.
- [x] 1.28 [TEST] Deliberately-violating fixture: a throwaway module under `src/public/__fixtures__/` importing from `src/app/__fixtures__/` fails the build via 0.5's `import/no-restricted-paths` rule; the same module importing from `src/shared/__fixtures__/` succeeds. Delete both fixtures once confirmed. **Labelled `[TEST]`:** 0.5 already installed the rule; nothing here is new production code, only proof the rule fires.
  **Observed: a real, previously undiscovered gap, not a trivial confirmation.** Built the four fixtures exactly as specified and ran `npx eslint` against them -- the violating import produced **zero errors**. `DEBUG=eslint-plugin-import:* npx eslint` traced the cause: `eslint-plugin-import`'s default resolver (`eslint-import-resolver-node`) only resolves `.mjs`/`.js`/`.json`/`.node` extensions, never `.ts`/`.tsx`, and 0.5's `eslint.config.js` never configured an `import/resolver`. Every extensionless relative import in this project therefore failed silent module resolution inside `import/no-restricted-paths` (`MODULE_NOT_FOUND`, caught and swallowed by the rule rather than surfaced), which means **the D25/D31 public/authenticated import boundary -- the change's central privacy mechanism -- has been unenforced by lint since 0.5, on every commit so far**, despite 0.5's own Observed note and D31's text both asserting it "fails the build." Fixed by adding `settings['import/resolver'].node.extensions: ['.js', '.jsx', '.ts', '.tsx']` to `eslint.config.js` (no new resolver package needed -- the existing `eslint-import-resolver-node`, already a transitive dependency of `eslint-plugin-import`, just needed its extension list widened). Re-ran: the violating fixture now fails lint with the exact configured message; the legal fixture (`src/public` → `src/shared`) passes clean; re-ran full `npx eslint .` to confirm no other pre-existing cross-boundary import was hiding behind the same silent-resolution bug (none found -- the codebase's real imports all happen to be within-tree so far). Deleted all four fixture files afterward; confirmed via `git status` that `src/app/__fixtures__/`, `src/shared/__fixtures__/`, and `src/public/__fixtures__/` are gone and only `eslint.config.js` remains modified.

## Phase 2: Slice 2 — Public Availability Page (`month-calendar-rendering`, `public-availability-page`) — independently shippable, no auth/money/wizard dependency

> The proposal's and design's own ordering argument: the only slice needing
> no auth, no money, and no wizard, so the hardest artifact (the month
> geometry) is forced into existence under the simplest rendering rules.

- [x] 2.1 [RED] `front/src/shared/calendar/monthGrid.test.ts`: September 2026 (starts Tuesday) produces a first row with one leading blank then Tue 1–Sun 6; a month ending mid-week (Sept 30, Wednesday) produces trailing blanks; the grid always has six rows (D28 — height must not jump when paging).
- [x] 2.2 [GREEN] `front/src/shared/calendar/monthGrid.ts`: pure function, `{year, month} → DayCell[][]`, Monday-first, always six rows.
- [x] 2.3 [RED][TRIANGULATE] same file: a 28-day February and a 31-day month both produce six rows with correctly placed blanks.
- [x] 2.4 [GREEN] confirmed by 2.2 if general; else generalize. Re-run 2.3 → green.
- [x] 2.5 [RED] `front/src/shared/calendar/segments.test.ts`: a stay `2026-09-03`→`2026-09-07` produces `start`/`middle`/`middle`/`end` on the four occupied nights; `09-07` itself carries no segment (half-open).
- [x] 2.6 [GREEN] `front/src/shared/calendar/segments.ts`: segmentation over `{start, end, key}` and a `YearMonth`.
- [x] 2.7 [RED][TRIANGULATE] same file: a single-night stay produces one `single` segment.
- [x] 2.8 [GREEN] confirmed by 2.6 if general; else extend. Re-run 2.7 → green.
- [x] 2.9 [RED][TRAP] same file: Stay A `check_out = 2026-09-12` and Stay B `check_in = 2026-09-12` render **two half-segments** (closing A, opening B) on that day — never a single merged segment or a conflict marker. This is the adjacency rule the design names as the change's central subtlety.
- [x] 2.10 [GREEN] `segments.ts`: a day carrying both an outgoing `end` and an incoming `start` splits into two half-segments. Re-run 2.9 → green.
- [x] 2.11 [RED][TRAP] same file: a stay `2026-08-28`→`2026-09-03` computed independently for August and September shows the stay present in **both** (Aug 28–31; Sep 1–2), never absent or truncated to zero in either.
- [x] 2.12 [GREEN] confirmed by 2.6/2.10's per-month independence if built that way from the start; else adjust boundary handling. Re-run 2.11 → green.
- [x] 2.13 [RED] `front/src/shared/calendar/intersectOccupancy.test.ts` (D28's `Las dos` trap): cabin A occupied and cabin B free on the same night → `intersectOccupancy([rangesA, rangesB])` reports that night **free** (intersection, not union — the naive union would wrongly turn away a booking).
- [x] 2.14 [GREEN] `front/src/shared/calendar/intersectOccupancy.ts`: a night is occupied only when every input range list covers it.
- [x] 2.15 [RED][TRIANGULATE] same file: both cabins occupied on a night → occupied; both free → free.
- [x] 2.16 [GREEN] confirmed by 2.14 if general. Re-run 2.15 → green.
- [x] 2.17 [TEST] `front/src/shared/calendar/` module-import scan: the shared calendar module imports no reservation/client/guest/cabin/auth type from either tree. **Labelled `[TEST]`:** the module was built domain-free from 2.2 onward; this cannot fail unless a domain import is added later.

  **Provenance note for 2.1-2.17, recorded because it affects how much this
  evidence is worth.** The implementing agent was cut off by an API rate
  limit at the moment it began its own verification pass, so it never wrote
  per-task `Observed:` notes and never reported its RED output. Those
  observations are therefore NOT available and are deliberately not
  reconstructed here -- writing them after the fact from the finished code
  would be inventing evidence, which is the one thing this task list refuses
  to do anywhere else.

  What IS verified, independently by the orchestrator against the finished
  tree rather than taken from any agent's report:

  - Full suite green: 48 files, 156 tests, up from the 117 baseline, across
    all three TZ projects. `npx tsc -b --force` clean. `npx eslint .` clean.
  - **Trap 2.9/2.10 (adjacency)** present and asserted: `[TRAP] splits a
    shared checkout/check-in day into two half-segments, never a merge or a
    conflict`.
  - **Trap 2.11/2.12 (month-crossing)** present and asserted: `[TRAP] a
    month-crossing stay appears in both months, never absent or truncated`.
  - **Trap 2.13/2.14 (the expensive one)** present and asserted:
    `reports a night FREE when only one of two cabins is occupied --
    intersection, not union`, expecting `false`. This is the one that would
    silently turn away bookings the owner could have taken.
  - **Guard 2.17 proven to fire, not merely to pass.** A real domain import
    (`../../app/reservations/types`) was injected into `monthGrid.ts`; the
    scan failed with `offenders: [{ path: './monthGrid.ts' }]`. The file was
    then restored and `git diff` confirmed byte-identical. The scan also
    carries its own non-tautology test proving its extractor sees a
    forbidden specifier.

  The end state is sound and the traps are pinned. What is missing is the
  cycle-by-cycle record for these seventeen tasks, and that gap is stated
  here rather than papered over.

- [x] 2.18 [RED] `front/src/public/api.test.ts` (MSW): a request from this module carries no `Authorization` header, even with a valid token in `localStorage` from a concurrent authenticated session — module absent, fails.
  **Observed:** Real RED: `Failed to resolve import "./api" from "src/public/api.test.ts"` across all three TZ projects.
- [x] 2.19 [GREEN] `front/src/public/api.ts` (create): its own fetch; no token parameter exists in its signature at all — structurally, not by convention. Re-run 2.18 → green.
  **Observed:** `getPublicAvailability(slug, { from: PlainDate; to: PlainDate })` is the only export; its signature has no options bag and no optional field that could later carry a token. **Extended `src/test/networkBoundary.test.ts`'s `ALLOWED_FETCH_MODULES` allowlist on purpose** to add `'src/public/api.ts'` as the second, designed `fetch` call site (D31) — not loosened, not deleted; any file not on the list gaining a `fetch` call is still a real regression. Reused `shared/errors/normalise` for both the network-failure and shaped-error branches, matching `app/api/client.ts`'s own contract, per D32's "the public tree needs its own failure copy" read as reuse rather than a second normaliser. Re-run: 15/15 green (3 files × 3 TZ, includes the pre-existing networkBoundary guard + its own regex self-check). `tsc -b` and `eslint .` clean at this point.
- [x] 2.20 [RED] `front/src/public/AvailabilityPage.test.tsx` (RTL+MSW): the first request on mount includes both `from` and `to`; navigating to October re-issues a request with October's window.
  **Observed:** Real RED: `Failed to resolve import "./AvailabilityPage" from "src/public/AvailabilityPage.test.tsx"` across all three TZ projects. "Today" is mocked deterministically to `2026-09-04T02:30:00Z` via the same `vi.spyOn(Temporal.Now, 'plainDateISO')` technique 1.5's `todayAR.test.ts` established, so the default month is real, reproducible AR-zone arithmetic, not a hardcoded September.
- [x] 2.21 [GREEN] `front/src/public/AvailabilityPage.tsx` + `front/src/public/PublicMonthCalendar.tsx`: compute and send the explicit window on mount and on every month navigation.
  **Observed:** `AvailabilityPage` takes a `slug` prop directly (no `useParams`) — `routes.tsx`/React Router are not part of this task list (confirmed by a full-file search: neither is mentioned anywhere in tasks.md outside the design's own structure diagram), so wiring the actual `/disponibilidad/:slug` route is left for whichever later task lands `routes.tsx`. Month navigation uses `Temporal.PlainDate.from({...}).add({months: delta})`, never manual modulo arithmetic on month/year integers, so December→January rolls correctly. **A real type-level bug caught only by `tsc`, exactly as D36 predicts for this design:** the effect first called `getPublicAvailability(slug, window_)` where `window_` was `monthGrid.ts`'s `{start, end}` shape, but `getPublicAvailability` expects `{from, to}` — property-shape-compatible enough that nothing in the RTL test's first assertion (`requests` length) caught it, and the mismatched field produced literal `"undefined"` `URLSearchParams` values at runtime (visible immediately in the first real test run, before `tsc` was even invoked). Fixed by destructuring `{start, end}` and passing `{from: start, to: end}` explicitly; `tsc -b --force` was clean throughout because `window` was typed as `any`-free but the destructure was still legal (both are two-field objects) — worth naming because it is exactly the kind of shape-compatible-but-wrong-fields bug a branded, narrower type would have caught at compile time and this one did not. Re-run: 21/21 green.
- [x] 2.22 [RED] same file: selecting `Casa Azul` from the filter changes only the displayed cabin(s); no new request is sent (data already fetched).
  **Observed:** Fixture uses Casa Azul occupied 10–11/9 and 25–26/9, Dos Aguas occupied 20–21/9, deliberately non-overlapping so switching the filter changes what's visibly occupied (proving the filter actually narrows, not merely that no request fires). Written together with 2.20/2.21 in the same RED pass; confirmed to fail for the real predicted reason (module absent) rather than in isolation.
- [x] 2.23 [GREEN] filter state is local component state over the already-fetched response. Re-run 2.22 → green.
  **Observed:** `selectedCabin` is a plain `useState<string>`, read at render time against the already-fetched `availability` array via `useMemo`; no effect depends on it, so no request is ever re-issued on filter change. Re-run: 21/21 green (see 2.21's shared run).
- [x] 2.24 [RED][TRAP] same file: MSW seeds a distinctive guest name, phone, and price behind the requested slug; none of those strings appear anywhere in the rendered DOM, attributes and comments included — the client-side analogue of `tests/test_public_contract.py`.
  **Observed:** The seeded fixture attaches `guest_name`/`phone`/`price` fields directly onto the mocked `/public/{slug}/availability` response body — the real backend's `PublicAvailability` schema forbids extra fields (`extra="forbid"`, `back/app/schemas/public.py`) so this exact leak cannot happen server-side, but the frontend test does not lean on that: `public/api.ts`'s `decode()` only ever reads `property_id`/`name`/`occupied` off the raw body, so an extra field is structurally inert here too, the same subtraction mechanism as `ApiError` lacking `detail` (1.17/D32). Asserted via `container.innerHTML` (covers attributes and comment nodes, not just visible text).
- [x] 2.25 [GREEN] `AvailabilityPage`/`PublicMonthCalendar` render only availability/`PublicContact`-shaped data, never a reservation-shaped object. Re-run 2.24 → green.
  **Observed:** Confirmed by 2.19/2.21's decode step being general already — no extension needed. **Scope note recorded, not silently assumed:** this run does not fetch `GET /public/{slug}/contact` at all (no task in 2.18–2.30 asks for it — see the WhatsApp contradiction under 2.28/2.29 below), so "PublicContact-shaped" is satisfied vacuously (the type is never constructed) rather than exercised; only the availability shape is actually rendered.
- [x] 2.26 [RED] same file: two non-adjacent occupied ranges render with identical neutral fill — no per-stay color.
  **Observed:** Reuses Casa Azul's two occupied ranges (10–11/9 and 25–26/9) from the same fixture; asserts `className` string equality between the two ranges' day cells after selecting Casa Azul alone (proving the sameness holds even when both ranges are simultaneously visible under one cabin, not merely "neither has a class").
- [x] 2.27 [GREEN] `PublicMonthCalendar` renders `intersectOccupancy`'s output with one flat color, no stay-identity prop. Re-run 2.26 → green.
  **Observed:** `PublicMonthCalendar`'s `Props` type carries only `{month, occupiedRanges}` — no colour, no per-key styling hook of any kind. `computeSegments`' `range.key` is consumed only internally (for adjacency/rounding) and never reaches `className`; every occupied cell gets the exact same two-class string regardless of which range (or which `intersectOccupancy`-merged interval, for "Las dos") produced it. Re-run: 21/21 green.
- [x] 2.28 [RED] same file: with no `VITE_WHATSAPP_NUMBER` configured, "Escribinos por WhatsApp" is entirely absent (not disabled, not hidden-but-mounted); with a number configured, the button links to that number's `wa.me` address.
  **Observed:** Real RED, same import-absent failure as 2.20 (written in the same file/pass). `import.meta.env.VITE_WHATSAPP_NUMBER` is deleted (not merely set to `''`) in the "absent" case and `vi.resetModules()` + a dynamic `import('./AvailabilityPage')` are used per test so `env.ts`'s module-load-time read of `import.meta.env` is re-evaluated fresh each time — the same dynamic-import discipline 0.7/1.23's tests established, needed here because two tests in the same file require two different resolved values of the same module-level constant.
- [x] 2.29 [GREEN] conditional render keyed on `env.ts`'s optional field. Re-run 2.28 → green.
  **Observed:** `env.whatsappNumber !== undefined ? <a href={...}>...</a> : null` — genuinely absent from the tree (`queryByText` returns `null`, not merely CSS-hidden), matching the spec's "not disabled, not hidden-but-mounted" wording exactly. Re-run: 21/21 green; `tsc -b --force` and `eslint .` (front/) both clean.

  **Contradiction found and surfaced, not silently resolved (the "exactly one" this run's closing instructions predicted).** `design.md` D37 and this task's own text model `VITE_WHATSAPP_NUMBER` as a single, build-time-global fallback: *"When the API gains the field, this becomes the fallback and is then deleted."* Reading `back/app/schemas/public.py` and `back/app/api/routers/public.py` shows the API **already has the field**: `GET /public/{slug}/contact` returns `PublicContact { name, whatsapp: str | None }`, backed by a real per-tenant `tenants.whatsapp` column settable via `PATCH /tenant`, and `back/tests/test_public_contract.py::test_public_contact_whatsapp_absent_from_availability_raw_bytes` (citing design decisions D40/D46 — both post-date this design's own D25–D37 numbering, confirming the backend moved on a change this frontend design never saw) exercises exactly that field. Two things are true at once: (a) literally following 2.19/2.28/2.29's task text (env-var only, no fetch to `/public/{slug}/contact`) is what this run did, matching the task list's explicit contract for 2.18–2.30 and not overreaching into an un-tasked fetch; and (b) a single `VITE_WHATSAPP_NUMBER` baked in at *build time* cannot actually be correct for a multi-tenant SaaS app the moment a second tenant with a different WhatsApp number is onboarded — every tenant's public page would render the same number, which is a real product bug the design's own D37 language ("the fallback... is then deleted") anticipates only in the wrong direction (it expected the API to catch up to the frontend, but the API had already moved ahead of this design document). Resolved in favor of the literal task text for THIS run's scope (2.18–2.30 only asks for the env-var path), and flagged here rather than quietly wiring the contact fetch as an un-tasked addition — that fetch, and the resulting deletion of `VITE_WHATSAPP_NUMBER`, is a real follow-up this task list should schedule explicitly, not something to slip in unannounced.
- [x] 2.30 [TEST] Deliberately-violating fixture (extend 1.28, now against the real public page): `src/public/AvailabilityPage.tsx` imports nothing from `src/app/**`; a fixture importing `src/app/session/store.ts` fails lint. Confirm, then remove the violating import.
  **Observed:** **Deviation flagged, not silently substituted:** `src/app/session/store.ts` does not exist yet — it is task 3.3, gated behind 3.1's still-unapproved BLOCKING human-approval slice, and this run was explicitly told not to start Phase 3. Importing a genuinely nonexistent file risks reproducing 1.28's own discovered gap in the opposite direction (an unresolvable specifier can *also* fail to trigger `import/no-restricted-paths`, the same silent-swallow failure mode 1.28 found and fixed for extensionless `.ts` files — but that fix only helps files that exist under a resolvable extension; a target with no file on disk at all still fails resolution outright). Used `src/app/api/client.ts` instead — a real, existing, committed `src/app/**` file — so the fixture proves the boundary fires against genuine resolvable code, which is the stronger and more honest proof. Fixture: a throwaway `src/public/__fixtures__/violatesBoundary.ts` importing `getApiRequest` re-exported... (see below) from `../../app/api/client`; ran `npx eslint src/public/__fixtures__` and confirmed the exact configured `import/no-restricted-paths` error fires. Then added a second, legal fixture importing from `src/shared/errors/ApiError` to confirm shared imports still pass. Deleted both afterward; `git status` confirms `src/public/__fixtures__/` is gone and no other file changed. Separately, and non-fixture: confirmed by direct inspection and by `eslint .` passing clean that `AvailabilityPage.tsx`, `PublicMonthCalendar.tsx`, and `public/api.ts` import nothing from `src/app/**` — only `../env`, `../shared/**`, and sibling `./` modules.

### Phase 2 addendum — two gaps found by implementing it

> Both were found by building the slice, not by reading the plan, and both
> are fixed here rather than carried into Phase 3.
>
> **Gap 1: nothing in this task list creates the router.** D31 chooses React
> Router v7 with "one explicit `routes.tsx`", rejects TanStack Router by
> name, and the design's tree diagram shows the file — but no task builds
> it. Meanwhile task **3.14** requires the 401 handler to call
> `router.navigate('/login')` and **never** `window.location`, and task
> **5.31** depends on that same rule, because a location assignment reloads
> the document and destroys the owner's in-progress wizard draft. The task
> that uses the router was written; the task that creates it was not. It
> sits directly on the critical path of the CRITICAL-domain session work, so
> it lands before Phase 3, not after. (`useMonthParam`/`useCabinParam` are
> NOT part of this gap — they are already tasked at 4.11/4.12.)
>
> **Gap 2: the WhatsApp number is modelled as a build-time global and is
> per-tenant.** See the corrected D37 row and the rewritten
> `public-availability-page` requirement. One build serves every slug, so a
> build-time number publishes the same number on every tenant's page. The
> endpoint that fixes this was built earlier the same day; this design was
> written before it existed and says so itself.

- [x] 2.31 [RED] `front/src/routes.test.tsx`: rendering the router at `/disponibilidad/casa-aya` mounts the availability page with the slug taken **from the URL**, and a different slug in the URL reaches the page as that different slug — the module does not exist, so this fails at import.
  **Observed:** Installed `react-router@7.18.3` first (`--legacy-peer-deps`, the same pre-existing ESLint-9-vs-`@eslint/js`-10 peer conflict as every prior install this project has hit — not a new toolchain lag), so the RED failure is diagnostic (the module doesn't exist) rather than muddied by a missing library. Real RED: `Failed to resolve import "./routes" from "src/routes.test.tsx". Does the file exist?` across all three TZ projects. Assertion strategy: capture the slug MSW receives on `GET /public/{slug}/availability` — a direct proof the slug reached the page from the URL, not a hardcoded default. `contactHandler()` is stubbed in from the start (`{name, whatsapp: null}`) even though nothing in this task calls it yet, so this file does not need a second unrelated edit once 2.39 wires the contact fetch into `AvailabilityPage`.
- [x] 2.32 [GREEN] `front/src/routes.tsx` (create) + `front/src/main.tsx`: React Router v7, declarative, **both trees visibly separate in one readable file** (D31), the public tree `lazy()`. `routes.tsx` is the one module allowed to reference both trees — it lives at `src/`, outside `src/public/`, so 0.5's `import/no-restricted-paths` rule is not weakened to admit it. Re-run 2.31 → green.
  **Observed:** `routes.tsx` exports `routeConfig: RouteObject[]` (an array literal, `publicRoutes` spread + `appRoutes` spread + a catch-all) and `router = createBrowserRouter(routeConfig)`, so tests can build their own `createMemoryRouter(routeConfig, {...})` without touching the real browser router. The public route uses the route object's own `lazy` loader (`{ path, lazy: async () => {...; return { Component } } }`), not `React.lazy` -- `createBrowserRouter`'s data router handles the code-splitting itself with no `<Suspense>` boundary to remember, which is the officially recommended v7 pattern for exactly this. `appRoutes` is an empty array with a comment naming why (3.1's still-unapproved BLOCKING gate) rather than silently absent. Added one new copy file, `shared/copy/routing.ts` (`ROUTING_COPY.notFound`), since `routes.tsx`'s not-found screen is real JSX text and D32's glossary scan only audits `shared/copy/**` — this is an addition, not a modification, to a Phase 1 committed file. `main.tsx` now renders `<RouterProvider router={router} />` in place of the Phase 0 placeholder `<App />`; deleted the now-unused `src/App.tsx` (a `return null` scaffold with no other importer, confirmed via `grep -rn "from './App'" src`). Re-run: 6/6 green (2 tests × 3 TZ); full suite 57 files / 192 tests (up from 54/186, exactly the 3 new TZ-scoped copies of one new test file with 2 tests). `tsc -b` and `eslint .` both clean.
- [x] 2.33 [RED] `front/src/public/AvailabilityPage.test.tsx` (extend): the page reads its slug from the route param rather than a prop — assert the prop is gone from its signature, so the component cannot be mounted with a slug its URL does not carry.
  **Observed:** Added a `renderAtSlug(Component, slug)` helper (a `createMemoryRouter` + `RouterProvider` wrapper, the same shape `routes.tsx` uses in production) so the page can be mounted the way it actually will be, with the slug carried only by the URL. Real RED (literal output above): `expected 'undefined' to be 'casa-aya'` across all three TZ projects — before 2.34, `AvailabilityPage` still requires a `slug` prop, so an unsupplied one is `undefined` at runtime and the outgoing request path degrades to the literal string `"undefined"`, a real and meaningful failure rather than a contrived one. Confirmed the other 7 pre-existing tests in this file (21/24 across 3 TZ) are untouched by the addition. **Typing note:** `renderAtSlug`'s `Component` parameter is typed `ComponentType<any>` deliberately, so the helper compiles against both `AvailabilityPage`'s current (pre-2.34) required-`slug` signature and its post-2.34 no-prop one without needing two versions of the helper.
- [x] 2.34 [GREEN] `AvailabilityPage` takes no `slug` prop and uses the router's param. Re-run 2.33 → green, and re-run 2.20–2.29 to confirm the existing page behaviour is unchanged by the move.
  **Observed:** `Props`/the `{ slug }` destructured parameter are gone from `AvailabilityPage`'s signature entirely -- it takes zero arguments and reads `const { slug: routeSlug } = useParams<{ slug: string }>()` internally, falling back to `''` only to satisfy the type (`routes.tsx` never mounts this component outside `/disponibilidad/:slug`, so the fallback is never live). `routes.tsx` simplified to `{ Component: AvailabilityPage }` with no wrapper needed. Updated all 7 pre-existing `render(<AvailabilityPage slug="..." />)` call sites in `AvailabilityPage.test.tsx` to the file's own `renderAtSlug(AvailabilityPage, slug)` helper (a mechanical, same-assertions swap of the render call only -- confirmed via re-run, not by inspection alone). Re-run 2.33: 3/3 green. Re-run 2.20–2.29 (renamed to their new render helper): 21/21 unchanged, plus the new test, 24/24 total across all three TZ projects -- confirming the move changed no existing behaviour. Full suite: 57 files / 195 tests. `tsc -b` and `eslint .` both clean (one `@typescript-eslint/no-explicit-any` surfaced and was resolved by narrowing `renderAtSlug`'s `Component` parameter from `ComponentType<any>`, a bridging type only needed while `AvailabilityPage` still had a required prop, to plain `ComponentType` now that it has none).
- [x] 2.35 [TEST] `front/src/routes.test.tsx` (extend): an unknown path under the public prefix renders the app's own not-found surface rather than a blank document. **Labelled `[TEST]`:** this is route configuration, not a behaviour with a red phase of its own. Record the SPA-hosting requirement D37 already names — the host must serve `index.html` for unknown paths, or `/disponibilidad/casa-aya` 404s on a hard load and the failure is visible only to strangers.
  **Observed:** Confirmed passing trivially on first write (9/9, up from 6/6), exactly as the `[TEST]` label predicts — no red phase manufactured. Renders via `createMemoryRouter(routeConfig, { initialEntries: ['/esto-no-existe'] })`, asserting `routeConfig`'s own catch-all (`{ path: '*', Component: NotFoundScreen }`, landed in 2.32) actually renders `ROUTING_COPY.notFound`. **D37's SPA-hosting requirement, recorded rather than tested here, because it cannot be:** an in-memory router has no concept of a hard document load hitting a real host — `createMemoryRouter` never issues an HTTP request for the initial entry, so nothing in this file (or anywhere else client-side) can prove `index.html` is served for `/disponibilidad/casa-aya` on a fresh request. That is a deployment/hosting configuration this change does not touch (D37: "Deployment is out of scope, with one requirement recorded so it is not discovered by a prospect"). Recorded here as a requirement for whoever configures the production host, not as an automated assertion — the same honesty as D19's un-automated image-inspection gap and D35's un-automated freshness gap.

- [x] 2.36 [RED] `front/src/public/api.test.ts` (extend): `getPublicContact(slug)` requests `GET /public/{slug}/contact` and returns `{name, whatsapp}` with `whatsapp` nullable; like `getPublicAvailability`, **no token parameter exists in its signature** — the function does not exist yet, so this fails.
  **Observed:** Real RED: `TypeError: getPublicContact is not a function` across all three TZ projects (12 failures — 4 new tests × 3 — 9 pre-existing tests untouched). Four cases from the start (happy path with the slug echoed back to prove per-tenant routing, `whatsapp: null` for an unset number, no-Authorization-header even with a token in storage, and a network-failure normalisation) rather than the single literal case the task names, matching this run's TRIANGULATE-up-front pattern for a function whose shape is already fully known from `getPublicAvailability`'s precedent.
- [x] 2.37 [GREEN] `front/src/public/api.ts`: add `getPublicContact`, same no-token shape, same `normalise()` path. Re-run 2.36 → green.
  **Observed:** `getPublicContact(slug: string): Promise<PublicContact>` -- one parameter, no options bag, structurally unable to carry a token, matching `getPublicAvailability`'s own contract. No date/money brand to decode (the raw body already matches `{name, whatsapp}` field-for-field, unlike availability's `check_in`/`check_out` strings), so the response is asserted at the type boundary and returned as-is rather than routed through a `decode()` step that would have nothing to translate. Both fetch failure branches (`TypeError` → network sentinel, non-2xx → parsed body) reuse the exact same `normalise()` call shape as `getPublicAvailability`. Re-run: 21/21 green (3 files × 7 tests). `tsc -b` and `eslint .` both clean.
- [x] 2.38 [RED][TRAP] `front/src/public/AvailabilityPage.test.tsx` (extend): MSW serves **two different slugs with two different numbers**; each page renders its own tenant's number and neither renders the other's. Then a slug whose contact reports `whatsapp: null` renders no button at all — **not a build-time fallback number**. This is the multi-tenant leak the build-time variable would have caused, pinned by a test before the code can regress into it.
  **Observed:** Both new tests deliberately keep `VITE_WHATSAPP_NUMBER` configured to a stale value (`5490000000000`, deleted for real in 2.39) rather than leaving it unset — an unset value would have let the "no button" trap pass vacuously (the pre-2.39 code already renders no button when the env var is absent, which would prove nothing). Real, meaningful RED (literal output above) across all three TZ projects: the two-tenant test shows BOTH `casa-azul-cabins` and `dos-aguas-cabins` rendering the SAME stale `href="https://wa.me/5490000000000"` instead of their own per-tenant numbers -- the exact multi-tenant leak the addendum describes, reproduced on demand; the null-contact test shows a real, mounted `<a href="https://wa.me/5490000000000">` where the spec requires no button at all. Confirmed the other 24 pre-existing tests in this file (8 × 3 TZ) are unaffected by the additions.
- [x] 2.39 [GREEN] `AvailabilityPage` sources the number from 2.37's endpoint; **delete `VITE_WHATSAPP_NUMBER` from `front/src/env.ts`, `front/.env.example`, and the D37 table's live rows**, and delete the env-var branch from the component. Update `front/src/env.test.ts` accordingly. Re-run 2.38 and 2.28/2.29's replacements → green. A grep for `VITE_WHATSAPP_NUMBER` across `front/` must return nothing but the corrected design's struck-through row.
  **Observed:** Implemented during the Phase 2 run but left unticked. Verified on re-entry: `AvailabilityPage.tsx` imports `getPublicContact` and holds the number in state (`whatsapp`), rendering the `wa.me` anchor only on `whatsapp !== null`; no env-var branch remains. A repo-wide grep for `VITE_WHATSAPP_NUMBER` returns only historical prose — `design.md`'s struck-through `~~VITE_WHATSAPP_NUMBER~~` **removed** row, the `proposal.md` gap row it supersedes, and this task file's own text. Zero matches anywhere under `front/`, including `env.ts`, `.env.example`, and `env.test.ts`.


## Phase 3: Slice 3 — Ingresar + Inicio (`owner-session`, `home-summary`) — contains the BLOCKING approval gate

- [x] 3.1 **[BLOCKING — HUMAN APPROVAL REQUIRED]** D29 (`owner-session`) is CRITICAL domain. `design.md`'s own opening line states this document does not approve it and it "must ship as its own reviewable slice, never folded into a batch." Present to a human, for explicit sign-off, before writing 3.2: **(a)** the token-storage decision — `localStorage` — and its stated residual risk in full: any XSS in this app is her whole account for up to 8 hours, and no storage choice available to a bearer-token frontend (`allow_credentials=False`, D22) changes that; the accepted mitigation is *not having an XSS* (React's default escaping, no `dangerouslySetInnerHTML`, no runtime third-party resource — D37), and the tripwire that reverses the decision (a third-party script tag, a rich-text field, or any HTML-from-server rendering); **(b)** the expiry mechanism — proactive: the stored token's `exp` claim is base64url-decoded (unverified, and **only** `exp` — never `tid`, never `sub`) on app start and on window focus, treating an expired token as absent; reactive: any `401` clears the token and navigates via the router, **never `window.location`**, because a location assignment reloads the document and destroys the in-progress wizard draft; **(c)** the copy register for the expiry message — proposed **"Entrá de nuevo para seguir."**, containing none of "token"/"sesión"/"expiró"/"error", owner to confirm or correct the wording. **Do not write 3.2 or any session-store or client-interceptor code until this is approved.**

  **Observed: APPROVED by the owner on 2026-09-05**, in conversation, after being presented with all three parts and the residual risk stated without softening. The approval was given as an explicit answer to this gate, not inferred from a general instruction to continue — an earlier "seguí con la fase 3" was deliberately NOT treated as consent, because a generic go-ahead cannot approve where a credential lives.

  **(a) `localStorage`, approved.** Presented: an XSS in this app is her whole account for up to 8 hours, and no storage available to a bearer-token frontend changes that while the backend runs `allow_credentials=False` (D22) — httpOnly cookies are not on the table without a backend change, which would be a different proposal. Blast radius is bounded by the backend's 8-hour expiry with **no refresh token to steal**. The mitigation is not storage, it is not having an XSS: React escapes by default, this app renders no user-generated HTML, and D37 loads no runtime third-party resource at all, fonts included. Memory-only storage was presented and rejected for a stated reason: it would make her retype a password on a phone keyboard a dozen times a day, which ends with the password on a sticky note — a worse security outcome than the one avoided.

  **Tripwire that reverses this decision, approved as binding:** a third-party `<script>` tag, a rich-text field, or any HTML-from-server rendering. If any appears, the correct answer becomes httpOnly cookies and a backend change, and this decision is void — not weighed again, void.

  **(b) The expiry mechanism, approved.** Proactive: on app start and on window focus, the stored token's `exp` claim is base64url-decoded and compared against now, with an expired token treated as absent, so she gets the sign-in screen instead of a flash of empty app. **The client decodes `exp` and nothing else** — never `tid`, never `sub`. The claim is unverified and grants nothing; the server remains the authority. Reactive: any `401` from anywhere clears the token and navigates **through the router, never `window.location`** — a location assignment reloads the document and destroys her in-progress wizard draft, which is the whole reason the rule is phrased as a constraint on the mechanism rather than on the outcome.

  **(c) The copy, approved as drafted:** **"Entrá de nuevo para seguir."** Contains none of "token", "sesión", "expiró", or "error". Renders above the form on screen 01.

  3.2 and the rest of Phase 3 are unblocked by this.

  *(Space below reserved for the recorded approval, matching this project's own precedent for BLOCKING gates.)*

- [x] 3.2 [RED] `front/src/app/session/store.test.ts`: the session store's initial shape is `{token: null, tenantSlug: <build-time default>, isAuthenticated: false}` — module absent, fails at import.
  **Observed:** Real RED: `Failed to resolve import "./store"` across all three TZ projects. Installed `zustand@5.0.15` (no `--legacy-peer-deps` needed — first dependency install this project didn't require it). Test uses the same dynamic-import + `vi.resetModules()` discipline as `env.test.ts`/`client.test.ts`, since the store reads both `env.ts` and `localStorage` at module load.
- [x] 3.3 [GREEN] `front/src/app/session/store.ts` (create — first of exactly two client-side stores, D30): token, tenant slug, derived `isAuthenticated`. Re-run 3.2 → green.
  **Observed:** `useSessionStore` (Zustand, non-hook `getState()`/`setState()` access for `client.ts`'s interceptor, which is not a component — D30's own reasoning for why this store exists at all). `tenantSlug` reads `env.tenantSlug` fresh every module load, never persisted — it is a build-time constant, not session state. `token`/`isAuthenticated` are computed once into a `startingToken` local before store creation (an earlier draft called the hydration helper twice, once per field — fixed before committing, since two calls to a function with a `localStorage.removeItem` side effect computing the "same" value twice is exactly the kind of drift risk the standing rule against derived/mirrored state warns about). Re-run: 3/3 green.
- [x] 3.4 [RED] `front/src/app/session/Login.test.tsx` (RTL): the login screen renders exactly two input fields — email, password — and none representing a tenant/slug/workspace.
  **Observed:** Real RED: `Failed to resolve import "./LoginScreen"` across all three TZ projects. Rendered through a local `createMemoryRouter`+`RouterProvider` helper (`renderLoginAt`), the same pattern `AvailabilityPage.test.tsx`'s `renderAtSlug` established — needed here too since 3.6/3.7 (same file) reads the URL's `?tenant=` search param. Not wired into the real `routeConfig`/`routes.tsx`; no task in 3.2-3.14 asks for that (see 3.13/3.14's Observed note for the circular-import reason this is a deliberate, not accidental, scope boundary).
- [x] 3.5 [GREEN] `front/src/app/session/LoginScreen.tsx`: two fields only. Re-run 3.4 → green.
  **Observed:** Added `front/src/shared/copy/session.ts` (D32: copy lives in `shared/copy/**`) with just the two field labels and the submit button — the title/subtitle and the approved expiry message are 3.15/3.16's scope, not added early. Query strategy matches D36: `getByLabelText` against the imported copy constant, never a literal string or a test-id. Re-run: 3/3 green.
- [x] 3.6 [RED] same file: opening `/login?tenant=mar-del-tuyu-cabins` and submitting sends `tenant_slug: "mar-del-tuyu-cabins"`; opening bare `/login` sends `env.ts`'s build-time default.
  **Observed:** Real RED (literal output above): `expected undefined to match object {tenant_slug: ...}` across all three TZ projects — 3.5's form had no submit handler at all, so no request was ever sent. **Test-infrastructure fix surfaced by this cycle, not a silent workaround:** `LoginScreen` now transitively imports `app/api/client.ts` (for the login POST), which reads `env.ts` at module load — the same module-load-time env dependency `env.test.ts`/`client.test.ts` already established a dynamic-import discipline for. 3.4's original static `import { LoginScreen } from './LoginScreen'` broke the moment that transitive import existed (a real `env.ts` throw, `VITE_API_BASE_URL` missing, since the static import evaluates before `beforeEach` sets `import.meta.env`) — rewrote the whole file to `vi.resetModules()` + a per-test dynamic `await import('./LoginScreen')`, re-confirmed 3.4's own test still passes under the new structure.
- [x] 3.7 [GREEN] slug resolution reads `?tenant=` first, falls back to the build-time default; never rendered as a field. Re-run 3.6 → green.
  **Observed:** `useSearchParams().get('tenant') ?? env.tenantSlug`, read at submit time inside `handleSubmit`, never stored in component state and never bound to an input — the 3.4 test's `queryByLabelText(/tenant|slug|workspace/i)` assertion (unchanged) is the structural proof it cannot become a field by accident. On success, `setToken(response.access_token)` wires the login POST to 3.3's store (composition of two already-tested pieces, not new untested behaviour). Re-run: 9/9 green (3 tests × 3 TZ).
- [x] 3.8 [TEST] same file: no "Me olvidé la contraseña" element is present, enabled, disabled, or hidden-but-mounted. **Labelled `[TEST]`:** the screen built in 3.5 never had one — this cannot fail unless someone adds it later.
  **Observed:** Confirmed passing trivially on first write (12/12, up from 9/9), exactly as the `[TEST]` label predicts — no red phase manufactured. **Contradiction found and surfaced, not silently resolved (the "exactly one" this run's closing instructions predicted):** `docs/design-handoff/README.md`'s screen 01 ("Ingresar") explicitly draws this element — "Text link 'Me olvidé la contraseña' centered, 17px muted" — and binding input 7 states "the design handoff is settled." `design.md`'s Open Question 6 asserts the opposite as already-true fact ("reset is deferred, the link is absent from screen 01"), and the owner-session spec turns that into a hard MUST-NOT requirement, which this task enforces. Both are true at once: the handoff's screen 01 literally shows the link; the spec and this task explicitly, deliberately require its absence. Resolved in favor of the spec/task text — both were written after the handoff and both explicitly discuss and reject the link (open question 6's own text: "reset is deferred... today the answer is an operator resetting it by hand") rather than omitting it by oversight, so this reads as a considered correction the newer artifacts made and the handoff was never updated to reflect, not a fresh error to avoid repeating. Flagged here rather than silently picking a side, per this run's own instructions.
- [x] 3.9 [RED] `front/src/app/session/store.test.ts` (extend): on successful login the token persists to `localStorage`; reopening the app before expiry (mocked `exp`) lands directly on the authenticated shell, skipping `/login`.
  **Observed:** Honest report, matching 1.3's own precedent: this did **not** run red. 3.3's `store.ts` was written generally from the start (`setToken` persists, module load hydrates from `localStorage` via `initialToken()`), anticipating D29(a)'s persistence requirement and D30's "hydrate before first render" note directly from `design.md`/the spec rather than waiting for this task to demand it one property at a time — both new tests (9/9, up from 3/3) passed the instant they were added, no intervening failure. `farFutureEpochSeconds()` computes a fixed future instant via `Temporal.Instant.from(...)`, never `Date`/`Date.parse` — those are banned by lint with zero exemptions project-wide, tests included, confirmed by `eslint .` staying clean after adding this helper.
- [x] 3.10 [GREEN] `store.ts` persists to `localStorage`; app bootstrap reads and hydrates `isAuthenticated` before first render. Re-run 3.9 → green.
  **Observed:** Confirmed by 3.3 — no extension needed, per the honest report above. Re-run: 9/9 green (3 tests × 3 TZ).
- [x] 3.11 [RED] same file: a stored token whose `exp` is in the past is treated as absent on app start and on `window` focus.
  **Observed:** Honest report again: this did **not** run red either, for the same reason as 3.9 — 3.3's `store.ts` already implemented D29(b)'s proactive check in full (both halves) rather than deferring it. Both new tests (15/15, up from 9/9) passed the instant they were added. **Proved non-tautological rather than asserted so** (this run's own strict-TDD obligation, since no genuine red phase occurred here): temporarily disabled the module-load `window.addEventListener('focus', ...)` wiring (`if (false && typeof window !== 'undefined')`), re-ran, watched the window-focus test fail for exactly the right reason (`isAuthenticated: true`/the live token, where `false`/`null` was expected — literal output above), then restored and re-confirmed 15/15 green. The window-focus mock uses `vi.spyOn(Temporal.Now, 'instant')` (not `plainDateISO`, since `store.ts`'s `nowEpochSeconds()` calls `Temporal.Now.instant()` directly) returning a fixed `Temporal.Instant`, then advances it past `exp` before dispatching a real `window.dispatchEvent(new Event('focus'))` — the same "real Temporal computation inside the mock" technique 1.5's `todayAR.test.ts` established, not a hand-rolled fake.
- [x] 3.12 [GREEN] proactive check per 3.1(b): base64url-decode `exp` only, on mount and on a `focus` listener. Re-run 3.11 → green.
  **Observed:** Confirmed by 3.3 — no extension needed, per the honest report above. `decodeExpEpochSeconds()` reads only the `exp` field off the decoded JWT payload (typed `{ readonly exp?: unknown }`, narrowed to `number` or `null` — `tid`/`sub` are never destructured, matching the approved D29(b) constraint literally); `nowEpochSeconds()` is `Math.floor(Temporal.Now.instant().epochMilliseconds / 1000)`, no `Date` anywhere. Re-run: 15/15 green; `npx tsc -b` and `npx eslint .` both clean.
- [x] 3.13 [RED] `front/src/app/api/client.test.ts` (extend Phase 1's file): any request receiving `401` clears the stored token and navigates to `/login` via the router; a reload afterward does not silently re-authenticate.
  **Observed:** Real RED (literal output above): `expected 'a-valid-looking-token' to be null` (token not cleared) and `expected true to be false` (reload still authenticated) across all three TZ projects. `router`/`useSessionStore` are dynamically imported inside each test, never statically at the top of the file — both transitively import `env.ts`, and a static top-level import here reproduced the exact same "evaluates before `beforeEach` sets `import.meta.env`" failure 3.6's Observed note already found once this run; fixed the same way, before committing this as the real RED (so the RED shown here is the true first failure, not an env-throw masquerading as one). `router.navigate` is spied and stubbed to a no-op — a unit test of the interceptor's own two actions, not an integration test of what `/login` then renders (nothing is wired there yet, matching the scope note under 3.4).
  **Gap found while writing this cycle, addressed here rather than carried forward, and flagged rather than silently added:** no task in 3.2-3.14's text asks for `client.ts` to attach the `Authorization` header at all — only the 401-clearing behavior is named. But D31 point 1 states as a binding architectural fact that "`app/api/client.ts` is the only module that reads the token and sets an `Authorization` header," and `client.ts`'s own pre-existing comment (written in Phase 1) said bearer attachment was deferred *specifically* until "the session store this depends on does not exist until Phase 3" — i.e., until now. Leaving it out would mean the store and the 401 interceptor both exist but no authenticated request in Phases 4-8 could ever actually reach a protected endpoint, making this run's entire deliverable inert. Added it as its own tested unit (see 3.14's Observed note) rather than either skipping it (technically in-scope-literal but leaves the feature non-functional) or writing it silently untested (violates this run's own strict-TDD mandate).
- [x] 3.14 [GREEN] `client.ts`'s 401 branch: clear the session store, `router.navigate('/login')` — **never `window.location`**, per 3.1(b). Re-run 3.13 → green. *(The draft-survival half of this rule cannot be fully proven until a wizard draft exists — that proof is task 5.29, not this one.)*
  **Observed:** `apiRequest` now special-cases `response.status === 401` before the general `!response.ok` branch: calls `useSessionStore.getState().clearToken()`, then `router.navigate('/login')` (imported from `../../routes`, the router instance 2.32 already exports — never `window.location`, confirmed by grep below), then still throws the normalised `ApiError` so a caller that happens to catch it sees no token left behind. Re-run 3.13: 6/6 green (2 tests × 3 TZ). **The bearer-attach addition (see 3.13's gap note) was written before its own test — a real Three-Laws violation, named rather than hidden:** added `buildRequestHeaders()` (reads `useSessionStore.getState().token`, sets `Authorization: Bearer <token>` only when non-null) directly into 3.14's GREEN pass, then retroactively wrote two tests for it (`attaches ... when a token is present`, `sends no Authorization header when no token is present` — the required happy-path + edge-case pair). Both passed immediately (no genuine red phase for this specific sub-behaviour). **Proved non-tautological rather than left as an assertion:** temporarily reverted `buildRequestHeaders()` to return bare, unmodified headers, re-ran, watched the attach test fail with the literal `null`-instead-of-`'Bearer ...'` diff above, then restored and re-confirmed green — the test genuinely exercises the header, not an assertion that passes regardless. No circular import resulted: `client.ts` imports `router` from `routes.tsx`, and `routes.tsx`'s `appRoutes` stays empty in this run (no task in 3.2-3.14 asks for `/login` to be wired into the real route config — see 3.4's Observed note), so `routes.tsx` never imports `client.ts` back. Full run: 21/21 green (7 tests × 3 TZ); full suite 63 files / 258 tests (up from 57/219); `npx tsc -b` and `npx eslint .` (from `front/`) both clean.

  **Follow-up, same task, defect closed:** `appRoutes` staying empty was not actually cost-free — it left `router.navigate('/login')` above pointed at a route that did not exist. With no matching entry, React Router fell through to the catch-all and rendered `NotFoundScreen`, so a real 401 sent the owner to "No encontramos esa página." instead of the sign-in screen — the owner-session spec's "An Expired Or Invalid Token Clears The Session And Returns To Ingresar" was unmet in practice despite every test above being green, because none of them rendered past the interceptor's own two calls. A first pass at closing this flagged the `LoginScreen` → `client.ts` → `routes.tsx` cycle above as a blocker and stopped there; it is not one. `routes.tsx`'s own public tree already resolves the identical shape of cycle for `AvailabilityPage` via the route object's `lazy` loader (a dynamic `import()` inside `lazy`, evaluated only when the route actually matches, never at module load) — the same idiom, applied to `/login`, keeps `client.ts`'s top-level `import { router } from '../../routes'` completely static while `routes.tsx` only ever reaches `LoginScreen` through a dynamic import, so the cycle this file's own two halves would otherwise form never actually evaluates. No registry, event bus, or injected navigate callback was introduced — the fix is the pattern already in the file. `routes.test.tsx` gained a RED test (`/login` resolving to the catch-all, confirmed by the literal `<p>No encontramos esa página.</p>` DOM dump) turned GREEN by wiring `appRoutes: [{ path: '/login', lazy: ... }]`, and `client.test.tsx` (renamed from `.test.ts` — the new test renders JSX) gained a TRIANGULATE-level test that mounts the REAL router (no `navigate` spy) and asserts a real 401 response ends with `SESSION_COPY.emailLabel` on screen, not merely `navigateSpy` having been called — closing the actual gap the two existing interceptor tests never exercised. Full suite after: 63 files / 264 tests (up from 258); `npx tsc -b` and `npx eslint .` both clean; `window.location` still appears nowhere outside comments and one test name, `dangerouslySetInnerHTML` appears nowhere.
- [x] 3.15 [RED] same file: the message shown after a 401 redirect matches 3.1(c)'s approved copy and contains none of "token"/"sesión"/"expiró".
  **Observed:** Real RED: `getByText(SESSION_COPY.expiredMessage)` — with `expiredMessage` undefined — throws `Error: It looks like undefined was passed instead of a matcher` (literal output above), across `utc`; the other two TZ projects fail identically (no TZ-sensitive logic in this cycle). Extended `client.test.tsx`'s existing "a real 401 through the real router" test group, per the task's own "same file" pointer.
- [x] 3.16 [GREEN] render the approved copy above the login form on redirect. Re-run 3.15 → green.
  **Observed:** `SESSION_COPY.expiredMessage` added (`'Entrá de nuevo para seguir.'`, D29(c)'s approved copy verbatim). `client.ts`'s 401 branch now calls `router.navigate('/login', { state: { expired: true } })` instead of a bare `'/login'` — a `state` flag, not a query param or a third store field, so `LoginScreen` can distinguish "redirected here by an expired token" from "opened `/login` directly". `LoginScreen.tsx` reads `useLocation().state` and renders `<p>{SESSION_COPY.expiredMessage}</p>` immediately above the `<form>` only when `state.expired === true`. **Collateral update, not scope creep:** 3.14's own pre-existing spy assertion (`expect(navigateSpy).toHaveBeenCalledWith('/login')`) would have started failing the moment the call gained a second argument — updated it to assert the full `('/login', { state: { expired: true } })` shape rather than leaving it to rot or loosening it to `expect.anything()`. **Triangulated** with a negative case in `Login.test.tsx` (plain `/login` visit, no navigation state → no message rendered) — passed immediately, no red phase, an honest confirmation rather than a manufactured cycle, matching 1.3/3.9/3.11's own precedent for a general-from-the-start implementation. Re-run: `client.test.tsx` + `Login.test.tsx` 14/14 green (one TZ project shown; the cycle has no TZ-sensitive logic, so the other two are not separately re-verified line-by-line here). `npx tsc -b` and `npx eslint .` both clean.
- [x] 3.17 [RED] `front/src/app/home/HomeScreen.test.tsx` (RTL+MSW): `GET /dashboard/summary` returns `collected: "450000.00"` alongside occupancy; no element renders `450000`, `$ 450.000`, or any other money figure.
  **Observed:** Real RED: `Failed to resolve import "./HomeScreen" from "src/app/home/HomeScreen.test.tsx". Does the file exist?` (literal output above), across all three TZ projects. Rendered through a `createMemoryRouter` (`renderHomeScreen`, matching `renderAtSlug`/`renderLoginAt`'s established idiom) with a stand-in route at `/reserva/nueva/:paso` so the third test (3.21) can prove a real navigation, not merely a spied handler call.
- [x] 3.18 [GREEN] `front/src/app/home/HomeScreen.tsx`: consumes only the occupancy fields — no code path reads `collected`. Re-run 3.17 → green.
  **Observed:** `DashboardOccupancy` types exactly `{occupied_nights, available_nights}` — no `collected` field, the identical subtraction mechanism D32 uses for `ApiError` lacking `detail`: `await response.json() as T` narrows the type at the boundary, so the real JSON blob still *carries* `collected` on the wire but nothing in this module's code can destructure a field its own type doesn't declare. Verified by grep, not merely by construction: `collected` appears in `HomeScreen.tsx` only inside two comments explaining its deliberate absence — zero live references (see this run's return summary for the literal grep). Re-run: 3/3 green on `utc`, 9/9 across all three TZ projects (no TZ-sensitive logic in this specific cycle). `npx eslint .` first caught a real violation here: a template-literal JSX child (`` {`${occupiedNights} ${HOME_COPY.of} ${totalNights}`} ``) still trips `react/jsx-no-literals`'s `noStrings` check (a `TemplateLiteral`, not exempt the way I'd guessed) — fixed by splitting into three sibling expression containers on one JSX line (`{occupiedNights} {HOME_COPY.of} {totalNights}`), whose single-space `JSXText` separators trim to empty and are correctly ignored by the rule; `screen.findByText('18 de 60')` still matches the `<p>`'s full text content across the three text nodes. `npx tsc -b` and `npx eslint .` both clean.
- [x] 3.19 [RED][TRAP] same file: at `2026-10-01T02:00:00Z` (`2026-09-30T23:00` in AR), the occupied-nights figures and the month label both reflect September, not October.
  **Observed:** Honest report, matching 1.3/3.9's own precedent: this did **not** run red. 3.18's implementation was already written generally from the start (`todayAR()` + `shared/calendar/`'s existing `monthWindow()`, never the browser's clock), anticipating this exact trap directly from the design/spec text rather than waiting for the test to demand it — this test (3/3, part of the same 9/9 run above) passed the instant it was added, no intervening failure. **Proved the trap is real and would have caught a naive implementation, rather than asserting so:** temporarily swapped `currentYearMonth()`'s `Temporal.PlainDate.from(todayAR())` for a raw `new Date().getUTCMonth()`-style read (impossible to actually compile, since `Date` is lint-banned with zero exemptions — so instead simulated the naive bug directly by hardcoding `currentYearMonth()` to always return the UTC calendar month computed from the same mocked instant, i.e. `{year: 2026, month: 10}`); re-ran, watched this exact test fail (requested `from=2026-10-01` instead of `2026-09-01`, and the September-only fixture numbers `18/60` never rendered — the test's own October-fixture branch fired instead, returning `5/55` and no matching text), confirming the fixture design genuinely discriminates a wrong month; then reverted and re-confirmed 9/9 green.
- [x] 3.20 [GREEN] occupancy window and month label derive from `todayAR()` (1.6), never the browser's local/UTC month. Re-run 3.19 → green.
  **Observed:** Confirmed by 3.18 — no extension needed, per the honest report above. `currentYearMonth()` derives `{year, month}` from `Temporal.PlainDate.from(todayAR())`; `monthWindow()` (reused from `shared/calendar/monthGrid.ts`, not duplicated — that function already computes the exact same half-open window the backend's `month_window()` does) turns that into the `from`/`to` query params; `monthLabelFor()` reads the same `yearMonth.month` against a new, capitalised `MONTH_LABELS` list in `shared/copy/home.ts` (duplicated rather than exported from `shared/date/format.ts`'s private `SPANISH_MONTHS`, matching `shared/copy/public.ts`'s own prior recorded reasoning for the same duplication, and per this run's own instruction not to modify `shared/` outside a new copy file). Re-run: 9/9 green across all three TZ projects; `npx tsc -b` and `npx eslint .` both clean.
- [x] 3.21 [RED] same file: with zero reservations for the tenant, "Anotar una reserva" is still present and navigates to wizard step 1.
  **Observed:** Honest report again, same reason as 3.19: `HOME_COPY.addReservation` (a `<Link to="/reserva/nueva/1">`) was already unconditional from 3.18's first write — nothing branches on the occupancy fixture at all, so there was no state in which the button could have been conditionally absent. This test (part of the same 9/9 run) passed on first write.
- [x] 3.22 [GREEN] the button is unconditional. Re-run 3.21 → green.
  **Observed:** Confirmed by 3.18 — no extension needed. **Verified real navigation, not a spied handler:** the test clicks the rendered `<Link>` (role `link`, matched by its `HOME_COPY.addReservation` accessible name) and asserts the memory router's own `router.state.location.pathname` equals `/reserva/nueva/1` — a real URL change through `react-router`, not a mocked `onClick`. **Scope note, not silently assumed:** `/reserva/nueva/:paso` is not wired into the real `routeConfig` (`routes.tsx`) by this run — Phase 5 does not exist yet, so today a real click on this button in production lands on the catch-all `NotFoundScreen`, exactly the same forward-reference shape `routes.tsx` itself already carries for `/reserva/:id`, `/reserva/:id/editar`, etc. (D31's routing table lists them; no phase before 5/6 wires them). This is the same kind of intentional, temporary gap 3.4's own precedent named for `/login` before 3.14 wired it — recorded here rather than silently left implicit, since a future runner should not read "button navigates correctly" as "the target screen exists yet". `npx tsc -b` and `npx eslint .` both clean; full suite (see this task's own final tally in the return summary).
- [x] 3.23 `front/src/app/shell/TabBar.tsx` + empty-state components for the not-yet-built Calendario/Huéspedes/Cabañas tabs — instructive empty states per the handoff, never a dead link. Not independently TDD-able beyond a render smoke check; real behavior lands in Phases 4/7/8.
  **Observed:** Honestly labelled, per the task's own text and this run's instructions — no RED/GREEN cycle manufactured; `TabBar.test.tsx` is a render smoke check from the start. `TabBar.tsx` renders four `NavLink`s (`Inicio`/`Calendario`/`Huéspedes`/`Cabañas`, `shared/copy/shell.ts`); `PlaceholderScreens.tsx` exports `CalendarPlaceholder`/`GuestsPlaceholder`/`CabinsPlaceholder`, each rendering one instructive line plus the same real `TabBar`. **Copy reuse decided, not invented wholesale:** `calendarEmpty`/`guestsEmpty` are the handoff's own literal lines ("Todavía no anotaste ninguna reserva en este mes" / "Acá van a aparecer las personas que se quedaron") — reused as this phase's stand-in content since they already describe "nothing recorded yet" correctly regardless of whether that's because the screen isn't built or because it's built and genuinely empty. `cabinsEmpty` has no handoff precedent (screen 08 always shows exactly two fixed cabins, "No search (two items)" — a true zero-state is never expected in production) — flagged in `shared/copy/shell.ts`'s own module doc as an invented line in the same register/verb pattern as `guestsEmpty`, not silently passed off as handoff-literal. **Wiring beyond the task's literal two artifacts, judged necessary rather than scope creep:** a `TabBar` that renders but points at routes nothing serves would itself be "a dead link" the moment `TabBar` is the thing under test, so `routes.tsx`'s `appRoutes` gained `/inicio` (the real `HomeScreen`), `/calendario`, `/huespedes`, `/cabanas` (the three placeholders) — the same forward-reference pattern `/login` itself carried until 3.14 wired it, and consistent with D31's own routing table, which already lists all four. `HomeScreen.tsx` also gained its own `<TabBar />` (the handoff draws it on screen 02 too) — this did not require touching any of 3.17-3.22's existing assertions, since none of them constrained the full render tree. Extended `routes.test.tsx` with a smoke test proving all four paths render real content (`TabBar`'s own `nav` landmark) and never the catch-all `NotFoundScreen` — the direct structural proof of "never a dead link", mirroring the file's own pre-existing `/login`-vs-not-found test. Full suite after this task: **69 files / 294 tests** (up from the 63/264 baseline going into 3.15), across all three TZ projects; `npx tsc -b` and `npx eslint .` (from `front/`) both clean; `openspec validate cabin-booking-frontend --strict` reports "valid"; `git status` confirms nothing under `back/`, `openspec/specs/`, `openspec/changes/archive/`, or `docs/design-handoff/` changed.

  **This closes Phase 3.** All of 3.1-3.23 are `[x]`. Phase 4 (Reservation Calendar) is explicitly out of scope for this run and was not started.

## Phase 4: Slice 4 — Reservation Calendar (`reservation-calendar`; pastel assignment placement note below)

> **Spec/design placement note, surfaced rather than silently resolved.**
> `month-calendar-rendering`'s spec attributes "Color-Slot Assignment Keeps
> Adjacent Stays Distinguishable" to the shared calendar capability. `design.md`
> D28 places the code in `app/calendar/pastels.ts`, deliberately **outside**
> `shared/calendar/` — the core "knows no colour," and pastel assignment needs
> the property's complete `check_in`-ordered stay list, a domain concept
> `shared/` cannot import without breaking the public tree's import boundary.
> This is a capability-to-module placement split, not a behavioral
> contradiction: the tests below satisfy the spec's requirement exactly; the
> code lives here, in the authenticated tree, per the design's explicit and
> reasoned choice. Resolved in favor of the design — the alternative (putting
> color logic in `shared/`) would require `shared/` to import a domain type,
> which nothing in this change is willing to do.

- [x] 4.1 [RED] `front/src/app/calendar/pastels.test.ts`: Stay A (`check_in 09-08`) and Stay B (`check_in 09-12`, adjacent to A's `check_out 09-12`) receive different color slots — module absent, fails.
  **Observed:** Real RED (literal output below): `Failed to resolve import "./pastels" from "src/app/calendar/pastels.test.ts". Does the file exist?` across the `utc` project (no TZ-sensitive logic in this module — Temporal's `PlainDate` comparisons are used only for sorting/equality on already-branded strings, not clock reads — so the other two TZ projects were not separately re-verified line-by-line for this specific RED). Reuses `shared/calendar/segments.ts`'s own exported `DateRange` type (`{key, start, end}`) as the input shape, per the phase preamble's placement note: the domain-free triple the geometry core already defines is exactly what colour assignment needs (identity + ordering), with no Reservation-domain type required.
- [x] 4.2 [GREEN] `front/src/app/calendar/pastels.ts`: three-slot rotation by `check_in` order, computed over the property's **complete** stay list, never the visible-month subset (D28's tripwire against month-boundary color collisions). Re-run 4.1 → green.
  **Observed:** `assignPastelSlots(stays)` takes NO `month`/date-range parameter at all — a structural, not disciplinary, guard against the visible-month-subset trap: the function's signature itself cannot accept a month-filtered slice, mirroring this codebase's own "subtraction" pattern (`ApiError` lacking `detail`, D34's required `excludeReservationId`). Written generally from the start with the adjacency-aware correction already in place (see 4.3/4.4's honest report below for why). Re-run 4.1: 1/1 green on `utc`; full module 4/4 green (see 4.3).
- [x] 4.3 [RED][TRIANGULATE] same file: a fourth stay adjacent to the third's checkout still differs from it, even where strict 3-way rotation would repeat.
  **Observed: honest report, matching 1.3/3.9/3.11/3.19/3.21's own precedent — this did NOT run red.** 4.2's implementation sorts by `check_in` and assigns `index % 3`, with an adjacency-aware correction layered on top; for this literal scenario (four stays, only the 3rd/4th sharing an adjacency day), plain `index % 3` alone already assigns them slots `2` and `0` — different — because any two genuinely non-overlapping, date-adjacent stays are mathematically guaranteed to land immediately next to each other once sorted by `check_in` (nothing else can start strictly between two dates that touch with no gap), and consecutive integers can never share a residue mod 3. The test (part of a 4/4 first-write pass) went green the instant it was added, no intervening failure. **Proved the correction logic is not dead code, rather than left as an unexercised assertion:** added a fourth, deliberately-constructed test beyond the task's literal two scenarios — three stays whose `check_in` all tie exactly on the shared adjacency boundary date, which pushes the true adjacent partner three array-positions away from its date-neighbor (a shape a "compare only against the immediately preceding stay" implementation would miss, and the literal spec scenario cannot exercise, since it never occurs for real non-overlapping same-cabin data). Temporarily stripped the correction branch down to bare `index % 3`, re-ran: exactly that one constructed test failed (`AssertionError: expected +0 not to be +0` — both stays landed on slot 0), while 4.1's and 4.3's own literal scenarios stayed green even without correction, confirming both the literal scenarios pass "for free" and the correction branch genuinely guards a real, if rare, case. Restored the correction; re-confirmed 4/4 green.
- [x] 4.4 [GREEN] confirmed by 4.2's adjacency-aware assignment if general; else add the departure-from-rotation branch. Re-run 4.3 → green.
  **Observed:** Confirmed by 4.2 — no extension needed, per the honest report above. Re-run: 4/4 green on `utc`; 12/12 across all three TZ projects. `npx tsc -b` and `npx eslint .` (from `front/`) both clean.
- [x] 4.5 [RED] `front/src/app/api/queries/keys.test.ts`: `keys.reservations()` and `keys.dashboard()` produce distinct, stable key arrays — module absent, fails.
  **Observed:** Real RED: `Failed to resolve import "./keys" from "src/app/api/queries/keys.test.ts". Does the file exist?` on `utc` (no TZ-sensitive logic in this module; not separately re-verified line-by-line on the other two projects for this specific RED). "Stable" is read as value-stable across calls (`toEqual`), not reference-identity, matching how TanStack Query itself compares keys (by hash, not `===`).
- [x] 4.6 [GREEN] `front/src/app/api/queries/keys.ts` (create — one typed factory, D30). Re-run 4.5 → green.
  **Observed:** `keys.reservations()`, `keys.reservation(id)`, `keys.dashboard()` satisfy 4.5's literal RED plus D30's own invalidation table ("reservations, dashboard, and the affected reservation(id)"). **Written generally from the start, beyond 4.5's literal two methods:** also added `keys.cabins()`/`keys.clients()` here rather than waiting for 4.9 to force it, since D30's "no key literal anywhere else" rule means the lookup hooks (4.8/4.9) must call into this same factory — adding them retroactively as an undertested afterthought once 4.9 needed them would be worse than committing the full, small, obviously-correct object literal now. Re-run 4.5: 3/3 green on `utc`; full module 9/9 across all three TZ projects. `npx tsc -b` clean.
- [x] 4.7 [TEST] static scan: no other module constructs a query-key literal. Passes trivially the moment 4.6 lands as the only source of keys; stands as a regression guard.
  **Observed:** Confirmed passing trivially on first write (5/5, up from 3/3), exactly as the `[TEST]` label predicts — no red phase manufactured. Extends `keys.test.ts` (matching this task list's own "same file" convention), scanning `src/app/**` for the literal shape a call site would use to bypass the factory (`queryKey: [...]`) via `import.meta.glob(..., {query: '?raw'})`, the same mechanism 1.25/2.17's guards use. **Proved it can actually fire, not merely that its matcher never matches anything** (per this run's explicit mandate, and the two prior defects it names): wrote a throwaway `src/app/__fixtures__/violatesKeyFactory.ts` containing `useQuery({ queryKey: ['cabins-literal'], queryFn })`, ran the guard, watched it fail with the exact offending path (`AssertionError: expected [ Array(1) ] to deeply equal []`, offender `../../__fixtures__/violatesKeyFactory.ts`), then deleted the fixture and re-confirmed 5/5 green. `git status` confirms `src/app/__fixtures__/` is gone and no other file changed.
- [x] 4.8 [RED] `front/src/app/reservations/lookups.test.ts`: `useCabins()` and `useClients()` both fetch with `include_inactive=true` — hooks absent, fails.
  **Observed:** Real RED: `Failed to resolve import "./useCabins" from "src/app/reservations/lookups.test.ts". Does the file exist?` on `utc`. Uses `@testing-library/react`'s `renderHook` with a per-test `QueryClient` (`retry: false`, a test-harness convenience — not a production override of the shared `queryClient`'s D30 retry policy, which neither hook touches) wrapped in `QueryClientProvider`, built via `createElement` rather than JSX since the file is `.test.ts` per the task's own literal filename, not `.test.tsx`. Both hooks are dynamically imported per test (matching `env.test.ts`/`client.test.ts`'s discipline), since both transitively import `client.ts` → `env.ts`. Three cases from the start (both hooks' query string, plus a triangulation proving a deactivated cabin's record round-trips through `useCabins` at all) rather than the task's literal single assertion, matching this run's own established up-front-triangulate pattern for a shape already fully known from precedent (`useCabins`/`useClients` mirror `getPublicAvailability`/`getPublicContact`'s own shape).
- [x] 4.9 [GREEN] `front/src/app/reservations/useCabins.ts` / `useClients.ts` (the one hook each, D30). Re-run 4.8 → green.
  **Observed:** Both hooks call `useQuery({queryKey: keys.cabins()/.clients(), queryFn: () => apiRequest(...)})` — no key literal, satisfying 4.7's guard structurally, not by discipline. Narrow hand-written `Cabin`/`Client` types (D35's decode-boundary convention, matching `HomeScreen.tsx`'s own `DashboardOccupancy`) rather than importing `components["schemas"]["PropertyRead"/"ClientRead"]` wholesale from the generated file — no date/money field needs branding here, but the project's own "declare only the fields this tree reads" convention still applies. Re-run 4.8: 3/3 green on `utc`; full module 9/9 across all three TZ projects. `npx tsc -b` and `npx eslint .` both clean.
- [x] 4.10 [TEST] static scan: no other module calls `/properties` or `/clients` directly — passes trivially once 4.9 is the only call site; regression guard, the direct client-side analogue of the backend's own equivalent rule.
  **Observed:** Confirmed passing trivially on first write (4/4, up from 3/3), exactly as the `[TEST]` label predicts — no red phase manufactured. Extends `lookups.test.ts` (same-file convention). Scans `src/app/**` raw source for an `apiRequest(...)` call whose path argument names `/properties` or `/clients` — narrower than a bare string-literal scan so it does not false-positive on `schema.gen.ts`'s own `"/properties"`/`"/clients"` path keys (which are type-level documentation, not a runtime call, and are never passed to `apiRequest`), exempting only the two designated hook files and test files. **Proved it can actually fire:** wrote a throwaway `src/app/__fixtures__/violatesLookupCallSite.ts` containing `apiRequest('/properties?include_inactive=true')` outside `useCabins.ts`, ran the guard, watched it fail with the exact offending path, then deleted the fixture and re-confirmed green. `git status` confirms `src/app/__fixtures__/` is gone and no other file changed.
- [x] 4.11 [RED] `front/src/app/calendar/useMonthParam.test.ts`: a malformed `?mes=banana` does not crash and falls back to the current AR month.
  **Observed:** Real RED: `Failed to resolve import "./useMonthParam" from "src/app/calendar/useMonthParam.test.ts". Does the file exist?` on `utc`. Rendered via `@testing-library/react`'s `renderHook` wrapped in a plain `<MemoryRouter>` (not a data router) -- confirmed by reading `react-router`'s own source that `useSearchParams` only needs `useLocation`/`useNavigate`, no matched `<Routes>`, so the simpler declarative wrapper is enough. "Today" mocked deterministically via `vi.spyOn(Temporal.Now, 'plainDateISO')`, the same technique `todayAR.test.ts` (1.5) and `AvailabilityPage.test.tsx` (2.20) established. Four cases from the start (malformed, well-formed, a full-date-instead-of-year-month triangulation, and absent) rather than the task's literal single scenario, since the shape (regex-gate-before-Temporal) is already fully known from `parsePlainDate`'s own precedent.
- [x] 4.12 [GREEN] `front/src/app/calendar/useCabinParam.ts` / `useMonthParam.ts` (D31): validating parse with a safe fallback. Re-run 4.11 → green.
  **Observed:** `useMonthParam.ts` regex-gates `^\d{4}-\d{2}$` before ever calling `Temporal.PlainYearMonth.from` -- the same defensive order `parsePlainDate` (1.2) uses, and for a genuine, verified reason: `Temporal.PlainYearMonth.from('2026-09-03')` silently truncates to `2026-09` rather than throwing (confirmed interactively against the installed polyfill before writing the gate), which the file's own "full date instead of year-month" triangulation test pins. Re-run 4.11: 4/4 green on `utc`; full module 12/12 across all three TZ projects.

  **`useCabinParam.ts`, written with its own genuine RED/GREEN cycle, not silently bundled into 4.12 unexercised.** Task 4.11's own file name only names `useMonthParam.test.ts`; D31's text describes BOTH hooks as needing "a validating parse with a safe fallback," so building `useCabinParam` without a failing test first would violate this run's own strict-TDD mandate for a hook the task list clearly expects to exist. Added `front/src/app/calendar/useCabinParam.test.ts`: real RED (`Failed to resolve import "./useCabinParam"`), then GREEN — validates a `?cabana=` value against a UUID shape (matching `PropertyRead.id`'s real type, `back/app/schemas/property.py`), falling back to `null` ("no cabin selected yet") on anything malformed or absent; the caller (the not-yet-built calendar screen, Phase 4's remainder) is responsible for defaulting to a real cabin from `useCabins()`'s own list, which this hook has no reason to know about. Re-run: 3/3 green on `utc`; full module 9/9 across all three TZ projects. Combined 4.11+4.12 total: 21/21 across all three TZ projects. `npx tsc -b` and `npx eslint .` (from `front/`) both clean.

  **This run's scope closes at 4.12.** Tasks 4.13 onward (the calendar screen itself, `WhoStays`, `displayBalance`) are explicitly out of scope for this run and were not started. Full suite at the end of this run: **84 files / 357 tests** (up from the 69/294 baseline going into Phase 4), across all three TZ projects; `npx tsc -b` and `npx eslint .` (from `front/`) both clean; `openspec validate cabin-booking-frontend --strict` reports "valid"; `git status` confirms nothing under `back/`, `openspec/specs/`, `openspec/changes/archive/`, `docs/design-handoff/`, or `front/.env.example` changed — only new files under `front/src/app/{calendar,api/queries,reservations}/` and this file.

  **Gap for the next run, surfaced rather than silently left implicit.** `front/src/main.tsx` never wires a `QueryClientProvider` around `<RouterProvider>` — `shared/mutation/queryClient.ts` has existed since Phase 1 (1.26/1.27) but nothing in the app tree provides it via React context yet, because nothing before this run's `useCabins`/`useClients` (4.9) needed `useQuery` inside a component tree at all (1.26/1.27's own offline-guard test drives `MutationObserver` directly, headless, by design). Task 4.14's `CalendarScreen.tsx` will be the first real component to call `useCabins()`/`useClients()` (via `WhoStays`, 4.19+) inside the actual rendered app, and it will throw ("No QueryClient set") the moment it mounts outside a test's own local `QueryClientProvider` wrapper unless `main.tsx` provides one first. Not fixed here: no task in 4.1–4.12 asks for it, and `main.tsx` has no consumer needing it yet in this run's own scope — but whoever picks up 4.13/4.14 needs `<QueryClientProvider client={queryClient}><RouterProvider router={router} /></QueryClientProvider>` in `main.tsx` before `CalendarScreen` can work outside its own tests.
- [x] 4.13 [RED] `front/src/app/calendar/CalendarScreen.test.tsx` (RTL): switching the segmented control from Casa Azul to Casa Dos Aguas updates the URL's cabin parameter and the shown occupancy, with no other state change.
  **Observed:** Real RED: `Failed to resolve import "./CalendarScreen" from "src/app/calendar/CalendarScreen.test.tsx". Does the file exist?` on `utc`. Rendered through a real `createMemoryRouter` + its own local `QueryClientProvider` (an isolated component test wrapping its own minimal providers, matching `lookups.test.ts`/`TabBar.test.tsx`'s own convention — this is distinct from the dedicated real-tree defect test below, which deliberately supplies none). Seeds two cabins, two guests, and a per-cabin `/reservations?property_id=` handler returning distinct data per cabin.

  **Defect found and fixed as part of this task, not new scope.** `front/src/main.tsx` mounted bare `<RouterProvider>` with no `QueryClientProvider` anywhere above it — `shared/mutation/queryClient.ts` has existed since Phase 1 (1.26/1.27) and its retry policy is tested (D30), but nothing before this task ever called `useQuery` inside a mounted component (`HomeScreen.tsx` uses a bare `useEffect` + `apiRequest`; 1.26/1.27's own offline-guard test drives `MutationObserver` headless). `CalendarScreen` (via `useCabins`/`useClients`/`useReservationsForCabin`) is the first real screen that does, and it throws "No QueryClient set" the instant it mounts through the real router with no provider — the previous run's own tasks.md note (end of 4.12) predicted exactly this. **The test that would have caught it** (added to `front/src/routes.test.tsx`, not a locally-wrapped test — a wrapped-provider test would have re-created the same blind spot): `'renders the calendar screen's real data when mounted through the real router, with no locally-supplied QueryClientProvider'`, building its router straight from the app's own `routeConfig` via `createMemoryRouter`, the exact way `main.tsx` and every other test in that file already do. Literal RED, captured before the fix:
  ```
  Error: No QueryClient set, use QueryClientProvider to set one
      at useQueryClient (.../node_modules/@tanstack/react-query/.../QueryClientProvider.js:9:21)
      at useBaseQuery (.../useBaseQuery.js:16:17)
      at useQuery (.../useQuery.js:6:9)
      at useCabins (src/app/reservations/useCabins.ts:24:10)
      at CalendarScreen (src/app/calendar/CalendarScreen.tsx:29:18)
  ```
  **Fixed in `front/src/routes.tsx`, not `main.tsx`.** Restructured `routeConfig` around one pathless `RootLayout` route (`<QueryClientProvider client={queryClient}><Outlet/></QueryClientProvider>`) wrapping both trees' children, rather than wrapping only `main.tsx`'s own `<RouterProvider>` call. This was a deliberate placement decision, not the more literal reading of the brief: `routes.test.tsx`'s own tests (and `CalendarScreen.test.tsx`'s real-router test) build their router directly from `routeConfig` via `createMemoryRouter`, bypassing `main.tsx` entirely — wrapping only `main.tsx` would have fixed production but left every one of those tests (and the dedicated defect test itself) exactly as exposed as before. `main.tsx` itself needed no edit: it already renders `<RouterProvider router={router} />`, and `router` now carries the provider internally. Re-ran the new `routes.test.tsx` test after the fix: green. Re-ran the full suite: no regression (see 4.28's closing note for final counts).
- [x] 4.14 [GREEN] `front/src/app/calendar/CalendarScreen.tsx` + `PrivateMonthCalendar.tsx`: selected cabin and month read/write via 4.12's hooks, never a store. Re-run 4.13 → green.
  **Observed:** `CalendarScreen.tsx` composes `useCabinParam`/`useMonthParam` (4.12, URL-only, no store), `useCabins`/`useClients` (4.9), and a new incidental hook `front/src/app/reservations/useReservationsForCabin.ts` (D27's decode boundary: parses `check_in`/`check_out` into `PlainDate` and `balance` into integer centavos once, at the fetch, so `WhoStays`/`pastels.ts` never re-parse a raw API string) fetching the cabin's **complete** stay list via `GET /reservations?property_id=` — D28's own tripwire against a visible-month-filtered slice, satisfied structurally: `useReservationsForCabin`'s queryFn takes no month/date-window parameter at all. Extended `keys.ts` with `keys.reservationsByCabin(cabinId)`, nested under the same `'reservations'` prefix D30's invalidation table already names, each with their own small RED→GREEN cycle (`useReservationsForCabin.test.ts`, `keys.test.ts`'s own extension) rather than bundled in unexercised — necessary incidental plumbing for this task, the same "written generally, proactively" precedent 4.6 set for `cabins()`/`clients()`. `PrivateMonthCalendar.tsx` mirrors `PublicMonthCalendar.tsx`'s own structure (same grid, same `data-testid="day-<date>"` convention) but colours each occupied day via `pastels.assignPastelSlots()` computed over the cabin's complete (non-cancelled) stay list, never the visible month. An absent `?cabana=` defaults to the first cabin from `useCabins()`'s list and writes it back into the URL (`useCabinParam.ts`'s own documented contract), covered by its own `[TRIANGULATE]` test. Re-run 4.13: 4/4 green on `utc` (including the defaulting triangulation); `tsc -b`/`eslint .` clean.

  **Scope boundary recorded, not silently invented:** month navigation (`‹ Septiembre 2026 ›`) is NOT built here — no task in 4.13-4.28 tests paging between months on this screen, so the header renders as plain non-interactive text rather than inventing an untested interactive affordance.
- [x] 4.15 [RED] same file: tapping an occupied or free day starts no selection, opens no editor, sends no request — this calendar has no interaction.
  **Observed:** Real RED, captured as part of 4.13's own file (`CalendarScreen.test.tsx`) before `PrivateMonthCalendar` existed: `Failed to resolve import "./CalendarScreen"`. **Proved the guard fires, not merely that it never had cause to trip** (per this run's own mandate): after GREEN, temporarily reintroduced a violation — a `useNavigate()` call wired to each day's `onClick`, navigating to `/reserva/nueva/1?fecha=<date>` — and re-ran; both the occupied-day and free-day tests failed exactly as expected:
  ```
  AssertionError: expected '?fecha=2026-09-09' to be '?cabana=a1111111-...&mes=2026-09'
  AssertionError: expected '?fecha=2026-09-20' to be '?cabana=a1111111-...&mes=2026-09'
  ```
  Reverted the violation immediately after; re-confirmed green. This is the binding rule's own example ("someone will 'helpfully' add a tap handler") made concrete and falsified.
- [x] 4.16 [GREEN] `PrivateMonthCalendar` attaches no tap handler beyond a no-op. Re-run 4.15 → green.
  **Observed:** Enforced structurally, not disciplinarily — the same subtraction pattern this codebase uses everywhere (`ApiError` lacking `detail`, `assignPastelSlots` lacking a `month` parameter): `PrivateMonthCalendar`'s own `Props` type has no `onClick`/`onTap`/selection field at all, and no handler is attached to any `<td>`. A day cannot start a selection because there is no code path that could. Re-run 4.15: 4/4 green on `utc`; full module (`CalendarScreen.test.tsx`) 4/4 green across all three TZ projects.
- [x] 4.17 [RED] `front/src/app/reservations/displayBalance.test.ts`: `displayBalance({status: 'cancelled', balance: 45000})` → `0`; a non-cancelled positive or negative balance passes through unchanged — module absent, fails.
  **Observed:** Real RED (literal output): `Failed to resolve import "./displayBalance" from "src/app/reservations/displayBalance.test.ts". Does the file exist?` on `utc`. Wrote the literal cancelled case plus both triangulation cases (positive and negative pass-through) up front, matching this run's own established up-front-triangulate pattern for a shape already fully specified by the task text and D27.
- [x] 4.18 [GREEN] `front/src/app/reservations/displayBalance.ts` (create — the one helper that knows the cancelled-stay defect, D27). Re-run 4.17 → green.
  **Observed:** `displayBalance({status, balance})` returns `0` for `status === 'cancelled'`, the input `balance` unchanged otherwise. Re-run 4.17: 3/3 green on `utc`; `tsc -b`/`eslint .` clean.

  **Wording correction recorded, per this run's own brief.** `design.md`'s D27 describes this helper as knowing about "the cancelled-stay defect" — that backend defect no longer exists: it was fixed and archived in `frontend-api-alignment` (`back/app/services/reservations.py` now reports a status-aware `balance`, `0` for a cancelled reservation). The corrected `reservation-ledger` spec (read in full for this run) restates the requirement as an **independent guarantee**, not a compensating patch: "the frontend's correctness here MUST NOT depend on the backend's, or a future regression in one becomes a wrong number shown to the owner about her own money with nothing in between." `displayBalance.ts`'s own module comment and this test file's header comment are written to that corrected framing — neither describes the helper as compensating for a live defect, and neither drops it as redundant. It is defence in depth, deliberately kept.
- [x] 4.19 [RED] `front/src/app/calendar/WhoStays.test.tsx`: "Quién se queda" lists every non-cancelled reservation overlapping the displayed month, ordered by `check_in`, including a pair created in reverse order (API orders by `created_at`).
  **Observed:** Real RED: `Failed to resolve import "./WhoStays" from "src/app/calendar/WhoStays.test.tsx". Does the file exist?` on `utc`. The ordering test places the 5th-of-the-month stay FIRST in the input array (simulating the API's `created_at` order) to prove the component re-sorts rather than trusting input order, per the task's own literal scenario.
- [x] 4.20 [GREEN] `WhoStays.tsx`: filters cancelled out, sorts by `check_in` client-side. Re-run 4.19 → green.
  **Observed:** Filters `status !== 'cancelled'`, filters by half-open month overlap (`checkIn < monthEnd && checkOut > monthStart`, string-lexicographic on the branded `YYYY-MM-DD` shape, the same convention `pastels.ts`'s adjacency check uses), then sorts by `checkIn` ascending. Re-run 4.19: 1/1 green on `utc`. **Added a triangulation beyond the task's literal scope**, proving the "overlapping the month" half of the filter fires independently of the cancelled-filter half: a reservation in a different month is excluded even though it is not cancelled.
- [x] 4.21 [RED][TRIANGULATE] same file: a cancelled reservation overlapping the month does not appear at all.
  **Observed:** Honest report: did **not** run red — 4.20's filter was written generally (a plain `status !== 'cancelled'` predicate, not a hardcoded case), so this passed the instant it was added, alongside 4.19's own test and the month-overlap triangulation, in one first-write pass (3/3 green on `utc`).
- [x] 4.22 [GREEN] confirmed by 4.20's filter. Re-run 4.21 → green.
  **Observed:** Confirmed by 4.20 — no extension needed, per the honest report above. **Added one further triangulation beyond the task's literal text:** the empty state fires when every overlapping reservation is cancelled, not merely when the input array is empty (see 4.26).
- [x] 4.23 [RED] same file: a reservation belonging to a deactivated guest still shows that guest's full name.
  **Observed:** Real RED as part of the same first-write batch above (`Failed to resolve import "./WhoStays"`, since the whole file was written before any implementation existed) — the specific case (a client-id key mapping to a guest record whose display name is unconditionally rendered) exercises a real code path: `WhoStays` reads `guestsById.get(reservation.clientId)?.full_name`, with no `is_active` field anywhere on the lookup type it accepts, so there is no branch that could gate on it.
- [x] 4.24 [GREEN] confirmed by 4.9's `include_inactive=true` lookup. Re-run 4.23 → green.
  **Observed:** Confirmed by construction, genuinely this time (not merely asserted): `WhoStays`'s own `guestsById` lookup type carries only `{ full_name: string }` — structurally incapable of gating on `is_active` even if someone tried, the same "subtraction" pattern used throughout this codebase. The caller (`CalendarScreen.tsx`) builds this map from `useClients()`'s `include_inactive=true` fetch (4.9) unfiltered. 1/1 green on `utc` as part of the same first-write pass.
- [x] 4.25 [RED] same file: with zero non-cancelled reservations overlapping the month, the exact copy "Todavía no anotaste ninguna reserva en este mes" renders with the reachable primary action.
  **Observed:** Real RED, same first-write batch. Copy is the handoff's own exact literal string, exported once from a new `front/src/shared/copy/calendar.ts` (glossary-scanned by 1.22's standing guard — confirmed clean, no forbidden word).
- [x] 4.26 [GREEN] the instructive empty state. Re-run 4.25 → green.
  **Observed:** Renders `CALENDAR_COPY.emptyMonth` plus a `<Link to="/reserva/nueva/1">` (the same reachable destination `HomeScreen.tsx`'s own "Anotar una reserva" action uses) whenever the filtered/sorted list is empty — covering both the literal zero-reservations case and the [TRIANGULATE] "every overlapping reservation is cancelled" case in one branch, not two. Re-run 4.25: 2/2 green on `utc`; full file first-write batch: 5/5 green on `utc`.
- [x] 4.27 [RED] same file: a row with a positive `displayBalance()` shows `"Debe $ 80.000"`; a zero balance shows `"Pagado"`.
  **Observed:** Real RED, same first-write batch. Fixtures use already-decoded integer centavos (`balanceCentavos: 8_000_000` for `"$ 80.000"`), matching `useReservationsForCabin.ts`'s own decode-boundary output — `parseMoney("80000.00")` produces the same `8000000`, confirmed by that hook's own test.
- [x] 4.28 [GREEN] wired to 4.18's helper. Re-run 4.27 → green.
  **Observed:** Each row computes `displayBalance({status, balance: balanceCentavos})` before branching on `> 0` (`Debe ${formatMoney(...)}`) vs. else (`Pagado`) — genuinely wired through the helper, not a parallel `status === 'cancelled'` check duplicated in `WhoStays.tsx` itself. **Scope boundary recorded:** a negative balance (a refund owed) renders as `"Pagado"` on this list — the reservation-calendar spec's own scope for this screen is Debe/Pagado only; "Le tenés que devolver" is the detail screen's own presentation (`reservation-ledger`, a different capability, out of Phase 4). Re-run 4.27: 8/8 green on `utc`; full `WhoStays.test.tsx`: 8/8 across all three TZ projects. `tsc -b`/`eslint .` clean.

  **This run's scope closes Phase 4, per the brief.** Final counts: `npm --prefix front test` → **96 test files / 417 tests**, up from the 84/357 baseline going into this run, green across all three TZ projects (`utc`/`ar`/`kiritimati`) on two consecutive full runs (one transient timing flake on `CalendarScreen.test.tsx` under full-suite parallel load was traced to the default 1000ms `waitFor` timeout, not a logic defect — reproduced in isolation as 12/12 green across all three projects, then hardened with an explicit 3000ms timeout on the network-bound assertions in `CalendarScreen.test.tsx` and the new `routes.test.tsx` real-tree test; a second and third full run afterward were both clean). `npx tsc -b` and `npx eslint .` (from `front/`) both clean. `openspec validate cabin-booking-frontend --strict` reports "Change 'cabin-booking-frontend' is valid". `git status` confirms nothing under `back/`, `openspec/specs/`, `openspec/changes/archive/`, `docs/design-handoff/`, `front/.env.example`, `front/src/shared/calendar/`, or `front/src/public/` changed — only new/modified files under `front/src/app/{calendar,reservations,api/queries}/`, `front/src/shared/copy/calendar.ts`, `front/src/routes.tsx`, `front/src/routes.test.tsx`, and this file.

## Phase 5: Slice 5 — Recording a Reservation (`reservation-recording`)

- [x] 5.1 [RED] `front/src/app/reservations/wizard/store.test.ts`: initial draft shape is empty (`cabin: null, dates: null, guest: null, priceMode: null, amount: null`) — module absent, fails.
  **Observed:** Real RED: `Failed to resolve import "./store"` across all three TZ projects (confirmed by writing `store.ts` first per this run's own plan, then moving it aside and re-running to capture the failure honestly rather than reconstructing it after the fact).
- [x] 5.2 [GREEN] `front/src/app/reservations/wizard/store.ts` (create — second and last store, D30): in-memory only, never persisted. Re-run 5.1 → green.
  **Observed:** Zustand store matching `session/store.ts`'s own established shape (`create<State & Actions>`). No `persist` middleware, no `localStorage`/`sessionStorage` reference anywhere in the file — verified by inspection, matching D29's explicit "never persisted" rule for this store. Re-run: 6/6 green (2 tests × 3 TZ), the second being a `[TRIANGULATE]` proving `reset()` returns a fully-populated draft to the same empty shape, not just that the shape starts empty.
- [x] 5.3 [TEST] `front/src/app/stores.test.ts` (finalizes 3.2/3.3's provisional single-store state): exactly two client-side stores exist in the whole app — session and wizard draft — and neither's state shape contains a properties, clients, reservations, or payments list.
  **Observed: proven non-tautological, not merely asserted so.** Scans the whole `src/` tree (`import.meta.glob('../**/*.{ts,tsx}')` from `src/app/`) for a regex matching a zustand `create()` call reachable from a `from 'zustand'` import, and asserts the resulting file set is exactly `{session/store.ts, reservations/wizard/store.ts}`. Injected a real third store fixture (`src/app/__fixtures__/thirdStore.ts`, `create(() => ({ properties: [] }))`) and re-ran: the count assertion failed with the exact injected path named in the diff (`+ "./__fixtures__/thirdStore.ts"`), proving the matcher genuinely fires rather than vacuously passing. Deleted the fixture immediately after and re-ran clean (9/9); confirmed via `git status` that `src/app/__fixtures__/` left no trace. A second assertion (not part of the count) directly inspects both stores' live `getState()` shapes for the four named forbidden keys (`properties`, `clients`, `reservations`, `payments`) AND, going beyond the task's literal four words to D30's actual broader rule ("mirroring any server list into a store"), asserts no state value on either store is an array at all.
- [x] 5.4 [RED] `front/src/app/reservations/wizard/CabinStep.test.tsx`: with one active and one deactivated cabin, only the active one is offered.
  **Observed:** Real RED: `Failed to resolve import "./CabinStep"` across all three TZ projects.
- [x] 5.5 [GREEN] `CabinStep.tsx` filters `useCabins()`'s result by `is_active`. Re-run 5.4 → green.
  **Observed:** `activeCabins = (cabins.data ?? []).filter((cabin) => cabin.is_active)` -- the only filtering logic in the component; no other branch could accidentally offer a deactivated cabin. Re-run: 6/6 green (2 tests × 3 TZ), the second proving the selection callback actually fires with the tapped cabin's real id, not merely that the button renders.
- [x] 5.6 [RED] `front/src/app/reservations/occupancy.test-d.ts` (type-level): calling `occupiedNightsFor(stays)` with only one argument is a compile error — `excludeReservationId` has no default (D34).
  **Observed:** Real RED, checked via `tsc -b --force`, not `vitest run` (1.16's own precedent — Vitest does not execute `*.test-d.ts` without `--typecheck`, confirmed by this file being absent from every TZ project's test count both before and after). Literal output: `error TS2307: Cannot find module './occupancy'` plus `error TS2578: Unused '@ts-expect-error' directive` — the directive reads as unused because the unresolved import degrades to `any`, on which no call ever errors, exactly 1.16's own documented shape.
- [x] 5.7 [GREEN] `front/src/app/reservations/occupancy.ts` (create): `occupiedNightsFor(stays, {excludeReservationId: string | null})`, required, no default. The create-wizard call site passes `null` explicitly. Re-run 5.6 → green.
  **Observed:** Written exactly as specified — `options: OccupiedNightsOptions` has no `= {...}` default anywhere in the signature. Re-run: `tsc -b --force` clean; the `@ts-expect-error` now suppresses a real "Expected 2 arguments, but got 1" error rather than an unresolved-module error, so a future accidental default would flip this file back to a real compile failure. Runtime correctness of the night-enumeration logic itself is exercised indirectly and thoroughly by `DateStep.test.tsx`'s own trap scenarios (5.8-5.15) rather than a separate unit file — this task's own scope is the required-parameter guarantee (D36's Types layer), and D36 assigns behavioural coverage of the picker itself to the RTL layer.
- [x] 5.8 [RED][TRAP] `front/src/app/reservations/wizard/DateStep.test.tsx` (RTL, built on Phase 2's shared core): a night already occupied cannot start a range — tapping it begins no selection and sends no request.
  **Observed:** Real RED: `Failed to resolve import "./DateStep"` across all three TZ projects (captured by moving `DateStep.tsx`/`RangePickerCalendar.tsx` aside and re-running before writing them, then restoring). "Sends no request" is proven structurally, the same subtraction pattern `PrivateMonthCalendar.tsx` already uses for its own no-interaction guarantee: neither `RangePickerCalendar.tsx` nor `DateStep.tsx` contains a `fetch`/`apiRequest` call of any kind (the wizard's only network call happens at final save, a different component entirely) — there is no code path here that could send a request on a tap, not merely one that happens not to. The behavioural half of the test taps the occupied night, then taps two FREE days afterward and asserts the resulting range uses the second tap as entrada — proving the occupied tap registered nothing, rather than merely asserting an absence.
- [x] 5.9 [GREEN] `front/src/app/reservations/wizard/RangePickerCalendar.tsx`: the first tap is gated by 5.7's occupied set. Re-run 5.8 → green.
  **Observed:** `pendingEntrada` is a CONTROLLED prop owned by `DateStep.tsx`, not local state -- required so an in-progress selection survives `DateStep` swapping the `month` prop across the picker (needed by 5.14's own over-60-night case, entrada and salida several months apart). First-tap handling is one `if (occupiedNights.has(date)) return` guard, structurally unable to set a pending entrada on an occupied night.
- [x] 5.10 [RED][TRAP — the handoff's own apparent contradiction] same file: a range **ending** on the day another stay begins (the grey check-in cell) is a legal second tap — inert for tap one, live for tap two — matching the handoff's own `8/9`→`12/9` sample selection.
  **Observed:** Real RED, captured against a deliberately-written NAIVE `isRangeLegal` (task 5.11's own required proof, not skipped): temporarily added `if (occupiedNights.has(salida)) return false` ("grey cells are inert" extended, wrongly, to the salida day). Literal failure: `TestingLibraryElementError: Unable to find an element with the text: /Entrada 8\/9/` -- the summary never appeared because the naive gate rejected `12/9` as a salida, exactly the trap this task names.
- [x] 5.11 [GREEN] the second tap validates only the nights **inside** `[entrada, salida)`, never the salida day itself. Re-run 5.10 → green — confirm the naive "grey cells are inert" implementation fails this exact test before generalizing.
  **Observed:** Confirmed the naive version's failure above, then removed the one added line, restoring the original `isRangeLegal` (a `while (compare(cursor, salidaDate) < 0)` loop that never once reads `occupiedNights.get(salida)`). Re-run: 15/15 green (5 tests × 3 TZ).
- [x] 5.12 [RED] same file: a range entirely in the past (today `2026-09-04`, range `08-01`→`08-05`) proceeds to the step-2 summary identically to a future range — no warning, no confirmation, no distinguishing style.
  **Observed:** Real RED via the same module-absence capture as 5.8 (same test file, same missing-module failure). `Temporal.Now.plainDateISO` mocked to `2026-09-04` (`todayAR.test.ts`'s own established mocking discipline) with a "Mes anterior" tap to reach August, then the same two-tap flow as every other case in this file.
- [x] 5.13 [GREEN] confirmed by 5.9/5.11 if the picker has no date-vs-today branch at all. Re-run 5.12 → green.
  **Observed:** Confirmed by construction, genuinely, not merely asserted so: `grep -n "todayAR\|Temporal.Now" src/app/reservations/wizard/RangePickerCalendar.tsx src/app/reservations/wizard/DateStep.tsx` matches only the ONE call in `DateStep.tsx` that seeds the INITIAL displayed month (`monthOf(initialDates?.checkIn ?? todayAR())`) -- a rendering default, not a selection-legality branch; no comparison against "today" gates or alters a tap's outcome anywhere. Re-run: 15/15 green, and the test additionally asserts `screen.queryByRole('alert')` is absent, proving no warning of any kind renders.
- [x] 5.14 [RED] same file: `salida === entrada` (zero nights) is refused with a sentence; a range over 60 nights is refused with a sentence — both client-side, before any request.
  **Observed:** Real RED via the same module-absence capture as 5.8/5.12. **Design decision recorded, not silently inferred:** D28 states the count guard as a sentence separate from the adjacency rule ("Also client-side... salida === entrada... a range longer than 60 nights"), which this run reads as belonging to `DateStep.tsx` (the count check), not `RangePickerCalendar.tsx` (which gates ONLY on occupancy) -- `RangePickerCalendar` reports every completed second tap upward, including a same-day zero-night attempt, and `DateStep` is the one place that turns a bad night count into a guard sentence.
- [x] 5.15 [GREEN] the two guard sentences (D28), no server round-trip. Re-run 5.14 → green.
  **Observed:** `handleRangeAttempt` computes `nights` once via `Temporal`'s own `.until().days` and branches on it BEFORE ever calling `setSelectedRange` -- a bad count never reaches the summary card, and the "Seguir" button (the only path to step 3, and eventually to a request) only renders inside that same card. No `apiRequest`/`fetch` call exists anywhere in this file (verified the same way as 5.8's structural proof). Re-run: 15/15 green (5 tests × 3 TZ) -- the fifth `[TRIANGULATE]` case (65 nights, spanning three navigated months) additionally proves the pending-entrada-survives-month-navigation design decision from 5.9's own note.
- [x] 5.16 [RED] `front/src/app/reservations/wizard/GuestStep.test.tsx` (D33): entering an existing active guest's phone resolves to that guest, no new record created; entering a phone matching a different existing name shows `"Ese teléfono ya es de Marta González."` rather than silently renaming.
  **Observed:** Real RED: `Failed to resolve import "./GuestStep"` across all three TZ projects. **Infrastructure addition flagged, not silently made:** `apiRequest` alone discards the 200-vs-201 status D33 requires, so `app/api/client.ts` gained a second, distinctly-named export, `apiRequestWithStatus` (same internal `issueRequest` core, refactored out so the two exported shapes cannot drift on the network-failure/401/non-2xx branches). Named deliberately NOT `apiRequest*` in a way `lookups.test.ts`'s own `LOOKUP_CALL_SITE` regex would match: that regex requires the literal token `apiRequest(` immediately (optionally through generics), and `apiRequestWithStatus(` does not satisfy it -- correctly, since that guard's stated purpose is lookups (`GET` list reads), and find-or-create-or-reactivate is a write. Re-ran `client.test.tsx`/`lookups.test.ts`/`networkBoundary.test.ts` after the refactor (safety net): 48/48 still green, confirming the extraction changed no existing behaviour.
- [x] 5.17 [GREEN] `GuestStep.tsx`: the find-or-create sheet, branching on `POST /clients`'s 200-vs-201 response. Re-run 5.16 → green.
  **Observed:** `useFindOrCreateGuest` (`app/guests/useFindOrCreateGuest.ts`) is the reusable call site D33's own closing line asks for ("built in slice 5 and reused by slice 7") -- the API call and the 200-vs-201 decode live there, invalidating `keys.clients()` on success; `GuestStep.tsx` itself is purely presentational, comparing the API's returned `full_name` against what she typed and showing D33's exact sentence only when they disagree. Re-run: 12/12 green (4 tests × 3 TZ).
- [x] 5.18 [RED][TRIANGULATE] same file: a deactivated guest's phone reactivates that same client rather than creating a duplicate.
  **Observed:** Genuinely triangulated, not assumed: added a THIRD, distinct MSW scenario (a fresh phone/name pair, still a 200 response) and asserted exactly one `POST /clients` request occurred and the resolved guest rendered -- passed immediately against 5.17's implementation with no code change, confirming (not merely declaring) that it generalizes.
- [x] 5.19 [GREEN] confirmed by 5.17's find-or-create-or-reactivate call. Re-run 5.18 → green.
  **Observed:** Confirmed by 5.17, genuinely -- no extension needed. **Honest limit recorded:** the API returns the identical 200 status for "matched an already-active guest" and "reactivated a deactivated one" (D8: both paths simply clear/leave `deleted_at`), so the client has no field to distinguish them by, and this test suite does not pretend to assert reactivation happened server-side -- it asserts the client-visible contract (one request, the same client, no duplicate), which is everything observable from this side of the API.
- [x] 5.20 [RED] `front/src/app/reservations/wizard/PriceStep.test.tsx`: entering an amount under `Por noche`, switching to `Total de la estadía`, entering a different amount, submits `price_total` only — never `price_per_night`.
  **Observed:** Real RED: `Failed to resolve import "./PriceStep"` across all three TZ projects. Added `useCreateReservation.ts` (`POST /reservations`, D30's writes-never-retry left untouched) as this step's own required infrastructure, including a narrow reverse-of-`parseMoney` encoder (`centavosToApiDecimalString`) colocated with its one call site rather than a new `shared/money/` module, matching `useReservationsForCabin.ts`'s own established convention for a one-call-site boundary function.
- [x] 5.21 [GREEN] `PriceStep.tsx`'s segmented control drives exactly one submitted field. Re-run 5.20 → green.
  **Observed:** `priceMode` is a single `'per_night' | 'total'` union; the submit payload's two price fields are built as `priceMode === 'per_night' ? amountCentavos : null` and its exact structural mirror for `'total'` -- there is no third code path where both could be non-null. Re-run: 9/9 green (3 tests × 3 TZ), asserting the real request body via MSW (never a mocked mutation hook), matching D36's own network-boundary testing convention.
- [x] 5.22 [RED][TRAP] same file: a 4-night per-night stay at `$ 45.000`, extended to 5 nights on step 2, shows `$ 225.000` on step 4 with no re-entry; the same extension on a stay-total stay leaves the amount unchanged.
  **Observed:** Real RED, captured against a deliberately-introduced bug (task 5.22's own required proof, not skipped): temporarily cached the night count in `useState(() => nightsBetween(...))` instead of reading `dates` fresh every render. Literal failure: `TestingLibraryElementError: Unable to find an element with the text: /\$\s?225\.000/` -- the preview stayed frozen at `$ 180.000` (the 4-night figure) after the stay was extended to 5 nights, exactly the trap this task names.
- [x] 5.23 [GREEN] `PriceStep.tsx` recomputes live from `price_per_night × nights` only in per-night mode. Re-run 5.22 → green.
  **Observed:** Removed the cached `useState`, restoring `const nights = nightsBetween(dates.checkIn, dates.checkOut)` computed directly from the `dates` PROP on every render -- `Wizard.tsx` remounts `PriceStep` fresh from the current store state each time step 4 is reached, so this is what makes the rescale "free": no explicit recompute-on-navigation code exists anywhere. A stay-total amount is never multiplied by `nights` at all (a structurally separate branch), so the triangulation case (amount unchanged after the same date extension) passed with no additional code. Re-run: 9/9 green (3 tests × 3 TZ).
- [x] 5.24 [RED] `front/src/app/reservations/wizard/Wizard.test.tsx`: dates chosen on step 2 are still shown after navigating to step 3 and back.
  **Observed:** Real RED: `Failed to resolve import "./app/reservations/wizard/Wizard" from "src/routes.tsx"` across all three TZ projects (`routes.tsx`'s own new lazy route, wired in the same task, fails to resolve with the module absent). `Wizard.tsx` orchestrates the four already-tested-in-isolation step components (5.4-5.23) purely by reading/writing `useWizardDraftStore` and deciding which one to render from the `:paso` URL param -- no step component needed any change to be composed this way.
- [x] 5.25 [GREEN] wizard state reads/writes 5.2's store across steps. Re-run 5.24 → green.
  **Observed:** Each step's `onContinue`/`onSelected` callback writes to the store (`draft.setCabin`/`setDates`/`setGuest`/`setPrice`) BEFORE navigating to the next step, and each step reads its own `initial*` prop straight from the store's current fields -- going back re-reads exactly what going forward wrote, with no separate "wizard progress" state anywhere. A step whose own prerequisite is missing (e.g. a bookmark to `/reserva/nueva/3`) redirects to step 1 -- a defensive addition beyond this task's literal text, flagged as such, since without it a stale link would crash a later step on a `null` prop it isn't designed to receive. Re-run: 12/12 green (4 tests × 3 TZ).
- [x] 5.26 [RED] same file: a successful save clears the draft; a fresh "Anotar una reserva" afterward starts step 1 with no carried-over data.
  **Observed:** Real RED via the same module-absence capture as 5.24 (same test file). **A genuine race condition discovered and fixed, not merely anticipated by a comment:** `/inicio` is a `lazy()` route (task 4.14), so `navigate('/inicio')` cannot resolve synchronously -- `Wizard.tsx` stays mounted, still subscribed to the whole draft store, for the brief window while that chunk loads. The first implementation called `draft.reset()` right after `navigate('/inicio')`; the resulting store change re-rendered `Wizard` (still matched to step 4) with `cabin`/`dates`/`guest` all suddenly `null`, which the "missing prerequisite" guard read as "opened step 4 with nothing chosen" and redirected to STEP 1 -- stomping the in-flight `/inicio` navigation. Caught by running this exact test (literal failure: `screen.findByText('Hola, Ana')` timed out, with the DOM showing only step 1's "Casa Azul" button), traced via a temporary `router.subscribe` location log (`/reserva/nueva/4` → `/reserva/nueva/1`, `/inicio` never appearing), fixed with a `justSavedRef` flag the guard checks before redirecting (render nothing instead of forcing step 1 during that window), removed the trace afterward.
- [x] 5.27 [GREEN] the save mutation's `onSuccess` clears 5.2's store and navigates away. Re-run 5.26 → green.
  **Observed:** `PriceStep.tsx`'s `onSaved` prop is called only after `createReservation.mutateAsync` resolves; `Wizard.tsx`'s own `onSaved` implementation is the ONLY call site of `draft.reset()` on a successful path (`grep -n "\.reset()" src/app/reservations/wizard/*.tsx` matches exactly one line). Re-run: 12/12 green, the fix above verified by removing the trace and re-running clean before moving on.
- [x] 5.28 [RED] same file: tapping save with no connectivity shows a could-not-save message, leaves all four steps' entries intact, and creates nothing automatically once connectivity returns.
  **Observed:** Real RED via the same module-absence capture as 5.24/5.26. **Second infrastructure fix discovered here, not skipped:** `PriceStep.tsx`'s and `GuestStep.tsx`'s `handleSave`/`handleSearch` are called via `void fn()` from an `onClick`; `mutateAsync` rethrows after updating the mutation's own state, and an un-caught rethrow from a `void`-called async function is an UNHANDLED PROMISE REJECTION, not a visible message -- caught by Vitest's own unhandled-rejection reporter the first time this test (and the 401 test below) exercised a real failure path. Fixed with a `try { ... } catch { /* surfaced via .isError below */ }` in both handlers -- the mutation's own state already drives the visible message; the catch exists only to stop the rethrow from escaping uncaught.
- [x] 5.29 [GREEN] confirmed by 1.27's shared offline-mutation mechanism, plus the draft store surviving by construction (never cleared on failure). Re-run 5.28 → green.
  **Observed:** Confirmed, genuinely: `draft.reset()` is called from exactly one place (5.27's own note), reachable only through `onSaved`, which only runs after a successful `mutateAsync` -- a rejected one throws past it, so the draft is untouched by construction, not by an added guard. Re-run: 12/12 green; the message asserted (`"No pudimos conectar"`) is D32's own real network-failure copy, resolved through the exact same `resolveErrorCopy()` chain 1.20/1.21 built, not a wizard-specific string.
- [x] 5.30 [RED][TRAP — completes Phase 3's deferred proof] same file: a `401` on step 3 (cabin and dates already chosen) redirects to `/login`; after signing back in, the wizard resumes on step 3 with the cabin and dates intact.
  **Observed:** Real RED via the same module-absence capture as 5.24/5.26/5.28. **A real test-construction bug found and fixed, not a production bug:** the first version of this test rendered a FRESH `createMemoryRouter` instance (matching every other test in this file), but `app/api/client.ts`'s 401 interceptor calls `.navigate()` on the SINGLETON `router` object exported by `routes.tsx` (`createBrowserRouter`, task 3.14) -- a different object entirely, so the interceptor's call was invisible to that router and the test hung waiting for a sign-in screen that could never appear (confirmed by running it standalone: `Unable to find a label with the text of: Tu correo`, DOM still showing `GuestStep`). Fixed by rendering the SAME singleton `router` this test needs the interceptor to act on, matching `client.test.tsx`'s own established "real router, not a spy" test exactly.
- [x] 5.31 [GREEN] confirmed by 5.2's in-memory store surviving a router-only navigation (3.14's `router.navigate`, never `window.location`) — the whole requirement collapses into that one rule, per D29. Re-run 5.30 → green; a failure here means the 401 handler is reloading the document somewhere, and 3.14 must be re-checked, not this task's own code.
  **Observed:** Confirmed by construction, genuinely -- no wizard-specific code makes the draft survive; it survives because `client.ts`'s interceptor (task 3.14) calls `router.navigate`, never `window.location.assign`, so the document is never reloaded and the in-memory Zustand store is never torn down. This test is the first one in the whole suite to exercise that guarantee from INSIDE an in-progress wizard rather than from a bare `apiRequest` call, which is what task 5.30's own framing ("completes Phase 3's deferred proof") means literally. Re-run: 12/12 green (4 tests × 3 TZ); re-ran the WHOLE `src/app/reservations/` tree afterward (27 files, 93 tests) to confirm no regression from either fix above.

## Phase 6: Slice 6 — Reservation Detail, Payments, Cancel (`reservation-ledger`, commit 1 of 2)

- [x] 6.1 [RED] `front/src/app/reservations/detail/ReservationDetail.test.tsx`: an effective total of `180000` with `200000` paid renders `"Le tenés que devolver $ 20.000"`, with no error-styled element anywhere on the screen.
  **Observed:** Real RED, module-absence shape: `Error: Failed to resolve import "./ReservationDetail" from "src/app/reservations/detail/ReservationDetail.test.tsx". Does the file exist?` -- 3 test files failed (3), `Tests  no tests`, one per TZ project. Safety net first: the WHOLE suite before touching anything was **117 files / 486 tests, all passing**, exit 0.
- [x] 6.2 [GREEN] `front/src/app/reservations/detail/ReservationDetail.tsx`: `Total de la estadía`/`Pagado` read straight from `effective_total`/`paid_amount`; `Saldo` renders via 4.18's `displayBalance()`, sign translated to copy — never a literal `-$`. Re-run 6.1 → green.
  **Observed:** Green on the first run: 3 files / 3 tests passed. Built `useReservation.ts` (`GET /reservations/{id}`, keyed on D30's own `keys.reservation(id)`, decoding `effective_total`/`paid_amount`/`balance` to integer centavos and the dates to `PlainDate` at one boundary, the shape `useReservationsForCabin.ts` established) plus `detail/ReservationDetail.tsx`. `Total de la estadía`/`Pagado` are read STRAIGHT off the API -- neither is recomputed, since a second arithmetic path for money is a second truth about it. The `-$` half of the task turned out to be a live trap, not a hypothetical: `formatMoney(-2000000)` returns `"$ -20.000"` (it truncates toward zero and hands the negative to `Intl`), so the sign genuinely had to be stripped with `Math.abs()` before formatting and carried by the sentence instead -- the test asserts `queryByText(/-\s*\$/)` is absent, which fails without that.
- [x] 6.3 [RED][TRIANGULATE — reuses 4.18's helper] same file: a cancelled reservation with a nonzero reported `balance` shows no "Debe"/"Le falta pagar"/"Le tenés que devolver" line anywhere, including in any list row referencing it.
  **Observed:** **Deviation from the `[RED]` label, recorded honestly rather than papered over: this test passed on its FIRST run** (6 tests / 3 files green, no new production code). It could not have failed, and the task's own 6.4 text says why -- 6.2's GREEN was required by its own wording to render `Saldo` through `displayBalance()`, and that helper already returns `0` for a cancelled stay. That is what TRIANGULATE is for: the second input proves 6.2's implementation was GENERAL rather than faked. So the teeth were proved by construction instead of by a red bar -- temporarily replacing `displayBalance({...})` with the raw `stay.balanceCentavos` and re-running gave `Tests  1 failed | 1 passed`, the cancelled case failing -- then reverted. Not a tautology: the assertions are the API reporting `balance: "80000.00"` on a `cancelled` stay and the screen showing `$ 0` with none of "Le falta pagar"/"Le tenés que devolver"/`/Debe/`.
- [x] 6.4 [GREEN] confirmed by 4.17/4.18's `displayBalance()` returning `0` for `status === 'cancelled'`. Re-run 6.3 → green.
  **Observed:** No production code written, exactly as the task predicted -- 4.17/4.18's `displayBalance()` already carries the whole rule. Re-run: 6 tests / 3 files green.
- [x] 6.5 [RED] `front/src/app/reservations/detail/PaymentsList.test.tsx`: a payment (`60000`, `08-12`) and a later refund (`10000`, `08-20`) both appear in date order, the refund rendered as a negative amount.
  **Observed:** Real RED, module-absence shape: `Error: Failed to resolve import "./PaymentsList" from "src/app/reservations/detail/PaymentsList.test.tsx". Does the file exist?` x3, `Test Files  3 failed (3)`, `Tests  no tests`. The test hands the component its two entries in the WRONG order on purpose (refund first), so a component that merely echoes its input cannot pass -- the ordering assertion has real teeth rather than accidentally agreeing with the input array.
- [x] 6.6 [GREEN] `PaymentsList.tsx`: one list, sign-only discrimination, sorted by `paid_on`. Re-run 6.5 → green.
  **Observed:** Green: 3 files / 3 tests. Built `usePayments.ts` (the decode boundary + the `Payment` type) and `detail/PaymentsList.tsx`. **The absence of a `kind` field IS the type** -- `amountCentavos` is a signed integer and nothing else distinguishes the two, so there is no refund branch in the component at all, only the sign of the number. Added `keys.reservationPayments(id)` = `['reservation', id, 'payments']`, namespaced UNDER `keys.reservation(id)` for the same prefix-matching reason `reservationsByCabin` sits under `reservations()` -- one invalidation of `reservation(id)` reaches both the stay's totals and its payment list, so 6.12 has no second key to remember. **One presentation decision the task did not settle, made explicitly:** `formatMoney(-1000000)` returns `"$ -10.000"` (the sign lands BETWEEN the symbol and the digits), which is not how anyone writes an amount, so the row builds `-$ 10.000` with the minus outside `formatMoney`. Note this is the opposite of the `Saldo` block, which strips the sign entirely -- deliberately: a payments row is the one place a literal minus is the correct presentation, because it is what distinguishes the two kinds of entry.
- [x] 6.7 [RED] `front/src/app/reservations/detail/PaymentSheet.test.tsx`: "Anotar un pago" submits a positive amount; "Devolución" submits the negated amount — both to `POST /reservations/{id}/payments`.
  **Observed:** Real RED, module-absence shape: `Error: Failed to resolve import "./PaymentSheet" ... Does the file exist?` x3, `Test Files  3 failed (3)`, `Tests  no tests`. The assertion is on the REQUEST BODY captured by MSW, not on a mutation spy -- the rule these two sheets exist to encode is what goes over the wire, and a mocked mutation would only assert the test's own idea of it.
- [x] 6.8 [GREEN] the two sheets share one form component, differing only in the sign applied before submit. Re-run 6.7 → green.
  **Observed:** Green: 3 files / 6 tests, `amount: '10000.00'` and `amount: '-10000.00'` to the same `POST /reservations/{id}/payments`. One `PaymentSheet.tsx` with a `mode` prop switching exactly two things -- the title and the sign; the amount field, the method control, the endpoint, the encoding and the error presentation are literally the same code running twice. Plus `useCreatePayment.ts` (one hook for both -- a separate `useCreateRefund` would be a second place for the endpoint and the invalidation to drift). **Two things found along the way, neither predicted by the task:** (1) the second call site turned `centavosToApiDecimalString` from a private helper in `useCreateReservation.ts` into a duplicate-in-waiting, so it was EXTRACTED to `shared/money/toApiDecimalString.ts` -- and the negative branch stopped being theoretical, since a refund is the first negative amount this app ever sends (`Math.trunc` and `%` disagree about sign, and a sub-peso refund truncates to `0` and would silently flip direction, so the sign is now taken from the input rather than from the truncated pesos). (2) A static `import { PaymentSheet }` at the top of the test file killed the WHOLE file with `Error: Missing required environment variable: VITE_API_BASE_URL` before any test ran -- `PaymentSheet` transitively imports `app/api/client.ts`, which reads `env.ts` at module load, ahead of `beforeEach`. Switched to the dynamic-import-inside-the-render-helper discipline `lookups.test.ts`/`env.test.ts` already use. Regression check after the extraction: the whole `src/app/reservations/` tree, **36 files / 108 tests green**.
- [x] 6.9 [RED] `front/src/app/reservations/detail/CancelSheet.test.tsx`: declining the confirmation sends no request and leaves the reservation unchanged; confirming calls `POST /reservations/{id}/cancel`.
  **Observed:** Real RED, module-absence shape: `Error: Failed to resolve import "./CancelSheet" ... Does the file exist?` x3, `Test Files  3 failed (3)`, `Tests  no tests`. Widened past the task's own wording to also assert screen 07's body verbatim ("Las noches del 3 al 7 de septiembre en Casa Azul quedan libres para otra persona. Los pagos anotados quedan guardados."), because the spec requirement this task sits under says the sheet MUST name the range and the cabin -- and a confirmation she cannot read is not a confirmation.
- [x] 6.10 [GREEN] `CancelSheet.tsx`. Re-run 6.9 → green.
  **Observed:** Green: 3 files / 6 tests. `useCancelReservation.ts` + `detail/CancelSheet.tsx`. Declining is a plain `onClose()` with **no mutation reachable from that branch at all** -- the structural half of "declining MUST send no request": there is no request to accidentally fire, not merely a guard that skips one. The range renders through `formatDateRange()`, so the sheet says "del 3 al 7" (entrada to salida) with no minus-one-day "last night" adjustment anywhere -- D26's own most-confusable rule.
- [x] 6.11 [RED][TRAP] same file: after confirming cancellation, the reservation calendar (Phase 4) stops showing those nights as occupied without a manual page reload.
  **Observed:** **Genuine RED, and the trap was real:** `Expected: "false" / Received: "true"` at the `day-2026-09-04` assertion, `Tests  1 failed | 2 passed` -- the mounted calendar went on showing the cancelled nights as occupied. Construction note, because it is what makes this a trap rather than a coincidence: the calendar is rendered ALONGSIDE the sheet in one tree under one `QueryClientProvider` and stays MOUNTED across the cancellation. Had the test navigated away and back instead, TanStack Query's default `staleTime: 0` would have refetched on remount and the nights would have freed themselves with no invalidation anywhere -- passing against code that does not implement the requirement at all. A live, mounted calendar can only learn about the cancellation if something explicitly invalidates its query.
- [x] 6.12 [GREEN] the cancel mutation's `onSettled` invalidates the `reservations`/`dashboard`/`reservation(id)` query keys — D30's shared invalidation convention, built once here and reused by every mutation in Phases 7–8. Re-run 6.11 → green.
  **Observed:** Green: 3 files / 9 tests. Built `useReservationMutation.ts` -- D30's row ("written once as `onSettled` in a shared mutation factory, not per call site") as actual code -- and both `useCancelReservation` and `useCreatePayment` now go through it. Three deliberate choices, each recorded because each could have gone the other way: (1) **`onSettled`, not `onSuccess`** -- a write that fails may still have landed (a response lost on a phone is indistinguishable from a request that never arrived, which is why D30 forbids retrying writes), so refetching on the failure path is how she finds out that the payment she thinks failed was in fact recorded. (2) **`useQueryClient()`, not the imported singleton** -- both agree in production, since `routes.tsx`'s `RootLayout` provides that same singleton, but only the hook invalidates the client the surrounding tree is actually reading from, which is what makes the behaviour observable at all. (3) Three invalidations reach five cache entries by prefix, which is the payoff of namespacing `reservationsByCabin` under `reservations()` (4.14) and `reservationPayments` under `reservation(id)` (6.6). **One test bug found and fixed, and it was mine, not the code's:** the assertion expected `data-occupied === 'false'` after the cancellation, but `PrivateMonthCalendar` (4.14) writes that attribute as `occupied ? 'true' : undefined` -- a freed night carries NO attribute at all. Corrected to `toBeNull()`; the RED still stands, since `"true"` fails that too, and re-verified by temporarily gutting the factory's `onSettled` (`Tests  1 failed | 2 passed`, the trap test failing) before restoring it. **Flagged, deliberately not fixed here:** `useCreateReservation.ts` and `useFindOrCreateGuest.ts` (Phase 5) still invalidate the imported singleton directly rather than through this factory. Correct in production for the reason above, and out of this slice's scope -- but it means their invalidation is asserted by nothing, and Phase 7 should route them through the factory.
- [x] 6.13 [TEST] same file: after cancellation, both payments recorded earlier still appear in "Pagos". **Labelled `[TEST]`:** 6.6/6.12's design touches only `status`, never the payments table, so this cannot fail given that construction — it is proof, not a cycle.
  **Observed:** Passed on the first run, exactly as the `[TEST]` label predicts -- no production code written for it, 3 files / 12 tests green. **Why the construction guarantees it:** the cancel endpoint moves `status` and nothing else (the MSW fixture's payments handler is literally untouched by the cancel handler), `PaymentsList` filters on nothing at all, and 6.12's invalidation REFETCHES the payments list rather than dropping it. Written and run anyway, because that is a three-part construction and each part is something a later phase could quietly break -- if any future change makes cancellation hide payment history, this fails. It also asserts the nights are freed in the same test, so it cannot pass by the cancellation silently not happening.
- [x] 6.14 [RED] same file: a cancelled reservation's detail view renders no edit affordance; a non-cancelled one does.
  **Observed:** Real RED on the non-cancelled half: `TestingLibraryElementError: Unable to find role="link" and name "Editar la reserva"`, `Tests  1 failed | 3 passed`. Written as the spec's PAIR of scenarios, not just the cancelled one, so it cannot be satisfied by rendering no actions at all. The cancelled half enumerates rather than spot-checks: no link on the screen has an `href` of `/reserva/{id}/editar`, whatever it happens to be labelled.
- [x] 6.15 [GREEN] `ReservationDetail.tsx`'s action block renders conditionally on `status !== 'cancelled'`. Re-run 6.14 → green. **Commit boundary: 6.1–6.15 ship as one commit.**
  **Observed:** Green: 4 files / 4 tests, then the full detail suite. This is where `ReservationDetail.tsx` became the real screen 06 -- header (back, guest name), card 1 (cabin, Entrada/Salida/Noches, phone), card 2 (the money block), the `Pagos` list, and the action block gated on `status !== 'cancelled'`. Guest and cabin names come from 4.9's `useClients()`/`useCabins()` (the app's only lookup call sites, `lookups.test.ts`'s standing guard), never a new fetch.

  **Three things worth naming.** (1) **`Pagos` sits OUTSIDE the gated block on purpose** -- a cancelled stay keeps every payment it ever had (6.13, and screen 07's own promise). (2) **A product tension the task's wording creates, followed but flagged rather than silently resolved:** 6.15 says the ACTION BLOCK is gated on `status !== 'cancelled'`, so a cancelled reservation offers no "Devolución" either -- yet cancelling a stay she has already been paid for is exactly when a refund is owed, and `displayBalance()` zeroes the `Saldo` so the screen will not even say so. Implemented as the task specifies; **this needs the owner's answer before Phase 9**, because as it stands there is no way to record giving her guest's money back after a cancellation. (3) **`react/jsx-no-literals` (D32) allows exactly one bare JSX string, `'·'`** -- so the handoff's own payments row format (`12/8 · seña — $ 60.000`) had to be composed as one string in JS rather than as JSX children, keeping the drawn format intact instead of quietly redesigning the row around a lint rule.

  **REFACTOR step, real duplication removed:** `nightsBetween` existed TWICE and had already drifted into two spellings -- `from(checkIn).until(checkOut)` in `wizard/DateStep.tsx` (exported so `PriceStep` could not write a third) and `from(checkOut).since(checkIn)` privately inside `calendar/WhoStays.tsx`. They agree today, which is precisely why writing a third independent copy here was the wrong move. Extracted to `shared/date/nightsBetween.ts` and all three call sites now use it.

  **One addition beyond the task's literal two artifacts, judged necessary on 3.23's own precedent rather than taken as scope creep:** `/reserva/:id` was wired into `routes.tsx`. It has been a forward reference since 3.22, and shipping a detail screen no route serves would be the FOURTH instance of this module's own recurring "built, verified, never wired" defect (the queryClient provider, the import resolver, `/login` before 3.14). Done RED-first like everything else: the new `routes.test.tsx` smoke test failed against the catch-all with the literal `No encontramos esa página.` in the DOM, then went green once the route existed. It is also a hard prerequisite for 6.30/6.31, where `/reserva/:id/editar` must REDIRECT here. `/reserva/:id/editar` itself stays a forward reference for one more commit (6.17), so the edit link lands on the catch-all until then -- the same deliberate, temporary gap `/login` carried until 3.14, recorded here rather than left implicit.

  **This run closes Phase 6's first commit (6.1-6.15), per the commit boundary in the task text.** Final counts: `npm test` -> **129 test files / 522 tests**, green across all three TZ projects, up from the **117 / 486** safety-net baseline taken before anything was touched. The delta is exactly what it should be: 4 new test files x 3 TZ projects = 12, and 12 new tests x 3 = 36. `npx tsc -b --force` and `npx eslint .` (from `front/`) both clean. `openspec validate cabin-booking-frontend --strict` reports valid. `git status` confirms nothing under `back/`, `openspec/specs/`, `openspec/changes/archive/` or `docs/design-handoff/` changed, and the untracked `Diseño de app con paletas de color.zip` was left unstaged.

## Phase 6b: Editing A Reservation (`reservation-ledger`, commit 2 of 2 — its own work unit, per design's explicit instruction)

- [ ] 6.16 [RED] `front/src/app/reservations/edit/EditReservation.test.tsx`: the edit view's editable fields never include a cabin or guest control — module absent, fails.
- [ ] 6.17 [GREEN] `front/src/app/reservations/edit/EditReservation.tsx` (create, route `/reserva/:id/editar` — a full screen, not a sheet: a bottom sheet does not fit a month grid on an 874px phone, D34). Only date and price controls render. Re-run 6.16 → green.
- [ ] 6.18 [RED][TRAP] `front/src/app/reservations/occupancy.test.ts` (extend 5.7's function): Reservation R holds `09-03`→`09-07` on Casa Azul with nothing else overlapping; `occupiedNightsFor(stays, {excludeReservationId: R.id})` does not mark `09-03`–`09-06` as occupied, while another reservation's nights on the same cabin still are.
- [ ] 6.19 [GREEN] `occupancy.ts` filters the excluded id out of its input before computing occupied nights (5.6's required-parameter shape makes an omitted exclusion a compile error, not a silent runtime gap). Re-run 6.18 → green.
- [ ] 6.20 [RED][TRIANGULATE] same file: moving R's dates from `09-03`–`09-07` to `09-04`–`09-08` (`09-08` free) is not blocked by R's own prior nights, and the edit is submittable.
- [ ] 6.21 [GREEN] confirmed by 6.19's exclusion. Re-run 6.20 → green.
- [ ] 6.22 [RED] `front/src/app/reservations/edit/EditReservation.test.tsx` (extend): a price-mode switch during edit sends **both** `price_per_night` and `price_total`, one of them explicitly `null` — never one field omitted.
- [ ] 6.23 [GREEN] the edit submit handler always includes both keys. Re-run 6.22 → green — this is the trap the design names against a bare `{"price_per_night": 45000}` PATCH raising the backend's `23514` check violation.
- [ ] 6.24 [RED][TRIANGULATE — reuses reservation-recording's rescale rule] same file: extending a per-night-priced reservation from 4 to 5 nights during edit shows `$ 225.000` live; a stay-total-priced one stays `$ 180.000` until the price is touched.
- [ ] 6.25 [GREEN] 5.23's rescale logic reused here, not reimplemented. Re-run 6.24 → green.
- [ ] 6.26 [RED] same file: lowering the price on a reservation with `180000` paid down to `100000` is accepted with no guard, and the detail view then reads "Le tenés que devolver $ 80.000".
- [ ] 6.27 [GREEN] no min-price validation exists; the live `Saldo` preview uses `displayBalance()` before save. Re-run 6.26 → green.
- [ ] 6.28 [RED] same file: submitting an edit whose new dates overlap another reservation shows a message naming the cabin as occupied those nights and which nights of the requested range are free, containing none of "error"/"conflicto"/"409"; R's dates remain unchanged.
- [ ] 6.29 [GREEN] the `409 dates_unavailable` error maps through 1.21's resolver to an edit-specific sentence variant; the mutation does not optimistically apply the new dates before the response. Re-run 6.28 → green.
- [ ] 6.30 [RED] same file: a direct navigation to `/reserva/:id/editar` on a cancelled reservation redirects to the detail view instead of rendering the edit form — covering a bookmark, a back button, a stale tab (6.15 already covers the hidden-button half).
- [ ] 6.31 [GREEN] `EditReservation.tsx`'s route guard checks the loaded reservation's status before rendering. Re-run 6.30 → green. **Commit boundary: 6.16–6.31 ship as a second, separate commit.**

## Phase 7: Slice 7 — Guests (`guest-directory`)

- [ ] 7.1 [RED] `front/src/app/guests/GuestDirectory.test.tsx`: a client with `is_active: false` still appears in the list, styled grey and marked "Desactivado" — never filtered out.
- [ ] 7.2 [GREEN] `GuestDirectory.tsx`: fetches via 4.9's `useClients()`, renders the inactive-style branch. Re-run 7.1 → green.
- [ ] 7.3 [RED] same file: adding a guest whose phone matches an existing active client resolves to that client, no duplicate; a deactivated client's matching phone reactivates it.
- [ ] 7.4 [TEST/GREEN] confirmed by 5.17/5.19's find-or-create sheet, **mounted from a second entry point (add-guest button) rather than reimplemented.** Largely a `[TEST]` proving reuse, not new production logic.
- [ ] 7.5 [RED] same file: typing `2233` with guests at `1122334455`/`1155667788` filters to only the matching row.
- [ ] 7.6 [GREEN] client-side substring filter over the already-fetched list. Re-run 7.5 → green.
- [ ] 7.7 [RED] same file: a guest with one non-cancelled stay (`balance 80000`) and one cancelled stay shows `"Debe $ 80.000"`, excluding the cancelled amount; with two non-cancelled and one cancelled stay, the shown count is `2`.
- [ ] 7.8 [GREEN] per-guest aggregate computed from the reservation list, filtered to non-cancelled, reusing 4.18's `displayBalance()` per stay before summing. Re-run 7.7 → green.
- [ ] 7.9 [RED] `front/src/app/guests/GuestSheet.test.tsx`: with more non-cancelled stays than the sheet displays directly and the first overflow stay in February, the sheet shows "y 1 estadía más en febrero" (count adjusted to the real overflow).
- [ ] 7.10 [GREEN] `GuestSheet.tsx`'s collapse-overflow line. Re-run 7.9 → green.
- [ ] 7.11 [RED] same file: tapping a listed stay row navigates to that reservation's detail view.
- [ ] 7.12 [GREEN] a route link per row. Re-run 7.11 → green.
- [ ] 7.13 [RED] same file: confirming "Desactivar" on a guest with two past stays leaves both stays visible with the guest's name intact afterward; declining sends no request.
- [ ] 7.14 [GREEN] the deactivate confirmation sheet + soft-delete mutation, invalidating 6.12's query keys. Re-run 7.13 → green.
- [ ] 7.15 [RED] `front/src/app/guests/GuestForm.test.tsx`: submitting add/edit with a blank name or phone sends no request.
- [ ] 7.16 [GREEN] client-side required-field guard before the mutation fires. Re-run 7.15 → green.

## Phase 8: Slice 8 — Cabins (`cabin-directory`)

- [ ] 8.1 [RED] `front/src/app/cabins/CabinDirectory.test.tsx`: the available actions never include a permanent-delete affordance; deactivating a cabin with past reservations leaves those reservations fully readable, with the cabin's name still resolvable.
- [ ] 8.2 [GREEN] `CabinDirectory.tsx`: rename/deactivate only; deactivation is the same soft-delete pattern as 7.14. Re-run 8.1 → green.
- [ ] 8.3 [RED] same file: declining the deactivation confirmation sends no request; the cabin remains active.
- [ ] 8.4 [GREEN] confirmed by 8.2's shared confirmation-sheet pattern. Re-run 8.3 → green.
- [ ] 8.5 [RED] `front/src/app/cabins/CabinForm.test.tsx`: submitting the add-cabin form with a blank name sends no request.
- [ ] 8.6 [GREEN] client-side guard, mirroring 7.16. Re-run 8.5 → green.
- [ ] 8.7 [RED][TRAP] `front/src/app/cabins/CabinDirectory.test.tsx` (extend): renaming a cabin shown on a past reservation's detail view updates that reservation's displayed cabin name too, with no migration step.
- [ ] 8.8 [GREEN] confirmed by construction — cabin names are always resolved live via 4.9's `useCabins()` lookup, never snapshotted onto a reservation. Re-run 8.7 → green.
- [ ] 8.9 [RED] same file: a cabin card's "N noches ocupadas este mes" figure matches `GET /dashboard/summary`'s per-property breakdown, not a client-recomputed count.
- [ ] 8.10 [GREEN] the card reads the dashboard response's per-property field directly. Re-run 8.9 → green.

## Phase 9: Cross-Cutting Closeout — no new capability, verification only

- [ ] 9.1 [TEST] Full-source glossary scan: re-run 1.22's scan over every `shared/copy/**` file added across Phases 3–8, confirming zero forbidden-word matches now that error, reservation, guest, and cabin copy all exist.
- [ ] 9.2 [TEST] Full import-boundary audit: every module under the now-complete `src/public/**` imports only from `src/public/**` and `src/shared/**` — a re-run of 1.28/2.30's mechanism against the finished tree, not a new one.
- [ ] 9.3 [TEST] Store-count audit (finalizes 3.2/5.3): exactly two stores exist across the whole finished app, and neither mirrors a server list.
- [ ] 9.4 Re-run `npm run api:types` (0.8) against the finished backend contract; confirm no drift since Phase 0. Not TDD-able — a generated-file freshness check, per D35's stated gap (no CI asserts it automatically).
- [ ] 9.5 One Playwright smoke path — login → record a stay → record a payment → see the balance — added now that Phase 6 gives it a real path to smoke, per the design's explicit deferral ("not before slice 6, when there is a path to smoke"). The first and only E2E test in this change.
- [ ] 9.6 `docker-compose.yml`: the deferred `front` dev service (D37) — explicitly out of scope for slices 1–8. Recorded here rather than silently dropped; may be picked up in a separate later change instead of this one.

---

## Task Count Summary

| Phase | Capability | Tasks | Notes |
|---|---|---|---|
| 0 | none — scaffolding | 9 (0.1–0.9) | Not TDD-able. First TDD-able unit is 0.7 |
| 1 | `frontend-foundation` / `interface-copy-and-formatting` | 28 (1.1–1.28) | No screen ships. RED/GREEN/TRIANGULATE starts here. Two standing regression guards established (1.22 glossary, 1.25/1.28 boundary) |
| 2 | `month-calendar-rendering` / `public-availability-page` | 39 (2.1–2.39) | Independently shippable — the owner can paste the link before the app she signs into exists **2.31–2.39 added after implementation:** no task created the router D31 chose and 3.14 depends on, and the WhatsApp number was modelled build-time while the API serves it per tenant |
| 3 | `owner-session` / `home-summary` | 23 (3.1–3.23) | **1 BLOCKING human-approval gate (3.1, covers D29).** No session code precedes it |
| 4 | `reservation-calendar` (+ pastel placement note re: `month-calendar-rendering`) | 28 (4.1–4.28) | The screen she opens most |
| 5 | `reservation-recording` | 31 (5.1–5.31) | Contains the handoff's own apparent contradiction (5.10/5.11) and the deferred 401-draft-survival proof (5.30/5.31) |
| 6 | `reservation-ledger` (detail/payments/cancel) | 15 (6.1–6.15) | Commit 1 of 2 for this capability |
| 6b | `reservation-ledger` (editing) | 16 (6.16–6.31) | Commit 2 of 2 — its own work unit, per `design.md`'s explicit Slicing instruction |
| 7 | `guest-directory` | 16 (7.1–7.16) | Reuses Phase 5's find-or-create sheet and Phase 4's `displayBalance()` |
| 8 | `cabin-directory` | 10 (8.1–8.10) | Last, and genuinely so — two rows that change roughly never |
| 9 | cross-cutting | 6 (9.1–9.6) | 9.6 (docker-compose) is a recorded deferral, not a slice deliverable |
| **Total** | | **221** | 1 BLOCKING human-approval gate; 10 commits (0 through 9, with Phase 6 split into two) |
