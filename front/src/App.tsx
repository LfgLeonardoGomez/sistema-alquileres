// Phase 0 is scaffolding only -- no screen ships yet (design's own Phase 1
// preamble: "ships no screen"). `routes.tsx` and the two route trees land
// once session/routing exist (Phase 3 onward).
function App() {
  return null
}

export default App
