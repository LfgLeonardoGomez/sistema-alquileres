import type { HttpHandler } from 'msw'

// Empty for now (task 0.4) -- populated per slice starting Phase 1, where
// each MSW-backed behaviour test adds the handler it needs.
export const handlers: HttpHandler[] = []
